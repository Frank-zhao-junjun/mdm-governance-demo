# 制造业基础数据治理技术建议书（V3）

> 版本：V3.0（实例版）　日期：2026-08-27
> 读者：信息化决策层、数据治理委员会、平台架构师、实施团队
> 编写原则：每个方案都给出可直接照做的实例——真实物料、真实 SQL、真实 API 报文、真实算法演算。所有"现状"结论可追溯至 RalphLoop MDM Governance 平台代码（commit `47c6281`）。

---

## 1. 背景与问题定义

### 1.1 业务背景

离散制造企业典型系统栈：ERP（SAP/Oracle/用友）+ MES + PLM + SRM + WMS + QMS + 数据中台/湖仓 + BI + AI 应用。物料主数据是采购、计划、生产、质量、财务、库存核算与报表的共同基础对象，记录规模通常几十万至千万级。

合规约束：**SAP 已于 2026 年 4 月公布第三方 AI Agent 不允许直连 SAP 系统**。所有 SAP 访问必须经 SAP BTP 受控通道，AI Agent 只能调用业务语义 API。该政策直接决定本文第 3 章的集成架构。

### 1.2 四类核心问题（附真实场景）

**问题 1：一物多码。** 同一颗"六角头螺栓 GB/T 5783 M8×30 8.8级 镀锌"，在某企业 SAP 里实际存在三条编码：

| 编码 | 名称（录入人随手写） | 创建系统 | 后果 |
|---|---|---|---|
| 10023417 | 六角头螺栓\M8*30\8.8 | SAP（2019，采购 A） | 采购价 0.42 元 |
| 10058722 | 螺栓 GB5783 M8×30 8.8级 | PLM 同步（2021，工程师 B） | 采购价 0.51 元，建了独立安全库存 |
| 20003105 | HEX HEAD BOLT M8X30 ZP CL8.8 | SRM（2023，供应商协同导入） | 无质量检验计划，IQC 漏检 |

后果量化：同一物料三条安全库存（积压约 3 倍）、三个采购价（价差 21%）、一条无质检记录（质量风险）。这类重码在 10 万级物料库中通常占 3%–8%。

**问题 2：关键字段缺失。** 某物料主数据导出抽样 1,000 条的典型结果：

| 字段 | 缺失率 | 直接后果 |
|---|---|---|
| 物料组（purchasing group） | 18% | 采购审批路由不到责任人，积压 |
| 基本计量单位换算 | 12% | 采购"箱"入库"个"，库存虚增 |
| 批次管理标识 | 9% | 质量追溯断链 |
| 计划交货期 | 31% | MRP 跑出错误的需求日期 |
| 制造商/制造商料号 | 44% | 备件无法溯源原厂，一物多码的温床 |

**问题 3：数据不一致。** 同一轴承在金标（拟）与各系统中的实测差异：

| 字段 | PLM | SAP | WMS |
|---|---|---|---|
| 名称 | 深沟球轴承 6204-2RS | 轴承6204 | BEARING 6204 |
| 单位 | 件 | 个 | PC |
| 重量 | 0.106 kg | 0.11 kg | 空 |

下游影响：数据中台按"件"汇总库存，WMS 的 PC 没被换算，库存报表差一个数量级；BI 会议上三个部门拿出三个库存数字。

**问题 4：多系统编码不统一。** 上述轴承：PLM 编码 `P-6204-2RS`、SAP `10045678`、WMS `WH-B-0455`、SRM 供应商料号 `SKF-6204-2RSH`。系统间靠 Excel 对照表人肉维护，对照表最后更新时间 8 个月前，责任人已离职。

### 1.3 治理目标与量化基线

形成"**存量治理 + 增量防错**"双闭环。非功能性验收基线（来自平台 PRD，作为各阶段底线）：

| 指标 | 目标值 | 说明 |
|---|---|---|
| 草稿保存响应 | P95 < 1s | 已有实现 |
| 提交校验响应 | P95 < 3s | 已有实现 |
| 重复预检响应 | P95 < 5s | pg_trgm 索引保障，见 §4.2 |
| 端到端闭环成功率 | ≥ 90% | 申请→发布全链 |
| SAP 直连违规数 | = 0 | 上线门禁 |
| 编码可解释率 / 发布可追溯率 / 重复预检覆盖率 | = 100% | 任一编码可回溯生成规则版本 |

---

## 2. 现状评估（基于代码事实）

平台已实现"申请 → 校验 → 审批 → 发布 → 同步"主链路（React 19 + FastAPI + SQLAlchemy 2.0，10 张表，~170 个后端测试用例）。逐项盘点：

### 2.1 已真实实现（可作为迭代地基）

| 能力 | 实现位置 | 说明 |
|---|---|---|
| 三级分类树 + 动态属性模板 | `models.py:73/92` | 分类自引用树；按分类定义字段/类型/必填/选项 |
| 申请-审批-发布状态机 | `api/applications.py` | draft→pending_admin→pending_dept→approved→publishing→published；管理员审批前重跑校验防快照过期；发布原子置位防并发 |
| 质量校验 + 规则配置化 | `services/material_validator.py`、`models.py:189` | 8 条内置规则，severity 分 blocking/warning，质量分=100−累计扣分；DB 可配开关/级别/扣分 |
| 原子编码生成 | `services/code_generator.py` + `crud.py:177` | pattern 占位符；`UPDATE...RETURNING` 单语句，并发唯一（有专项测试） |
| 金标 + 版本快照 | `models.py:205/253` | 修订三段审批、失效两段、回滚取上一已发布快照；不可变快照 + 版本链 |
| 全链路审计 | `services/audit_service.py` | 15 种步骤，执行人/状态/完整 details JSON |
| 发布任务持久化 | `models.py:337` | 五态状态机 + 超时标记 + 人工重试端点 |
| 附件安全 / JWT+RBAC | `api/applications.py`、`core/auth.py` | 拒可执行类型、强制下载头；无免认证回退，生产缺密钥 fail-fast |

### 2.2 已实现但有缺陷（本轮要修）

| 能力 | 缺陷（代码级） |
|---|---|
| 重复预检 `duplicate_detector.py` | 只匹配 `material_name`；用空格分词算词重叠率，**中文无空格，实际等价于"前 4 字前缀+精确匹配"**；结果不阻断提交 |
| 治理规则 `governance_rules` | 只存开关/severity/扣分元策略，规则逻辑硬编码，DB 加不了新规则类型 |
| BTP 发布 `btp_mock.py` | 纯 Mock，sleep 0.5s 返回伪造 sync_id |
| OM 同步 `openmetadata_sync.py:65-73` | 只拼 FQN 返回成功，未真实写入 |
| 发布重试 | 仅人工触发，无后台 worker/指数退避/死信队列 |
| 元数据治理 | 单只读聚合端点，血缘是静态构图非真实解析 |

### 2.3 完全缺失（本轮要建）

跨系统编码映射表与 API、一物多码合并流程（候选池/工作台/字段级决策/回写）、存量清洗（批量扫描/工单/回写适配器）、语义与指标治理、AI Agent、用户库入库 + SSO、金标生命周期前端 UI、PostgreSQL + Alembic。

---

## 3. 总体架构

### 3.1 三层分工

```
┌─────────────────────────────────────────────────────────────┐
│  观察面/目录层：OpenMetadata                                  │
│  资产目录、字段元数据、血缘展示、质量测试报告（只读展示）       │
├─────────────────────────────────────────────────────────────┤
│  执行层/交易层：RalphLoop MDM（本平台，唯一权威源）            │
│  申请/审批/编码/校验/归并/金标/工单/规则引擎/审计              │
├─────────────────────────────────────────────────────────────┤
│  受控集成层：SAP BTP                                          │
│  所有 SAP 读写的唯一通道；Agent 安全边界；回执与 traceId 台账  │
└─────────────────────────────────────────────────────────────┘
        ▲                                    ▲
        │ 业务语义 API（白名单）              │ BTP OAuth Scope 最小权限
   AI Agent（建议层，不持有 SAP 凭证）    MES / WMS / SRM / 数据中台
```

OpenMetadata 偏元数据治理/目录/血缘，承担不了 MDM 交易中台；BTP 是合规通道。交易闭环必须自研承载。

### 3.2 四条铁律

1. **OpenMetadata 不做交易主库**——物料几十万至千万级，不得逐条建成 OM 实体；OM 只管理"资产/字段"级元数据
2. **OpenMetadata 不直连 SAP**——SAP 资产/血缘/回执只经 BTP 台账进入
3. **AI Agent 不直连 SAP**——只能调业务语义 API，工具白名单 + 最小权限 + 人工确认 + 审计
4. **所有 SAP 写入可追溯**——requestId / traceId / btpTraceId / 回执四段串联

### 3.3 数据一致性原则

- 本平台为主数据源，OpenMetadata 为只读展示层
- 同步异步化（不阻塞审批发布）、最终一致、幂等、批量优先；冲突生成对账任务
- 本地事务 + 补偿，不引入分布式事务
- 发布走事件驱动 + Outbox（PostgreSQL Outbox 表），7 种事件：MaterialCreated / MaterialUpdated / MaterialPublished / MaterialInvalidated / MaterialMerged / MaterialRolledBack / MappingUpdated

Outbox 表结构（实例）：

```sql
CREATE TABLE outbox_events (
    id            BIGSERIAL PRIMARY KEY,
    event_type    VARCHAR(32)  NOT NULL,      -- MaterialPublished 等 7 种
    aggregate_id  VARCHAR(64)  NOT NULL,      -- golden_record_id
    payload       JSONB        NOT NULL,      -- 含版本号、checksum
    trace_id      VARCHAR(64)  NOT NULL,
    status        VARCHAR(16)  NOT NULL DEFAULT 'pending',  -- pending/sent/failed
    retry_count   INT          NOT NULL DEFAULT 0,
    created_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    sent_at       TIMESTAMPTZ
);
CREATE INDEX idx_outbox_pending ON outbox_events (status) WHERE status = 'pending';
```

写入方式：业务事务内同时写业务表 + outbox（本地事务保证一致）；独立 relay 进程轮询 pending 事件投递，消费方按 `(event_type, aggregate_id, payload->>'version')` 幂等去重。

### 3.4 部署拓扑

| 环境 | 构成 | 资源 |
|---|---|---|
| PoC/开发 | FastAPI + SQLite；OpenMetadata docker-compose（OM Server + PostgreSQL + ES + Airflow） | OM 侧 ≥ 6 GiB 内存 + 4 vCPU（建议 8 GiB+），Docker ≥ 20.10；Windows 走 WSL2 |
| 生产 | FastAPI + PostgreSQL（JSONB + pg_trgm）+ Kafka/Outbox + Camunda/Flowable + SSO/OIDC；OM 外部组件版 | 内网需私有镜像仓库/离线包；ES 内存单独规划 |

---

## 4. 迭代目标与技术方案（每项目标附实干实例）

### 4.1 目标 A：唯一物料身份（Golden Material ID）+ 跨系统编码映射

**现状**：`material_code` 即唯一身份（原子序列生成），无跨系统映射。

**方案 1：编码规则实例化。** 编码 pattern 用"类型-分类-流水"三段式，示例：

```
pattern: {类型}-{大类}{小类}-{流水:5}
类型码:  RM=原材料  SF=半成品  FG=成品  AU=辅料  SP=备件
实例:    RM-10302-00123   ← 原材料/大类10(金属件)/小类02(紧固件)/第123号
         SP-30701-00008   ← 备件/大类30(电气)/小类01(电机)/第8号
```

`code_rules` 表增加 `rule_version`、`effective_from`、`effective_to`；编码生命周期升级为**模拟（submit 前预演）→ 预留（审批期间占位）→ 确认（发布生效）→ 释放（驳回/作废回收）**四态，消除断号争议。`crud.increment_seq`（crud.py:177）的 `UPDATE...RETURNING` 原子性已有专项测试，继续沿用。

**方案 2：跨系统映射表（DDL 可直接执行）：**

```sql
CREATE TABLE cross_system_code_mappings (
    id                BIGSERIAL PRIMARY KEY,
    golden_record_id  BIGINT       NOT NULL REFERENCES golden_records(id),
    system_name       VARCHAR(32)  NOT NULL,        -- SAP-S4HANA / MES / WMS / SRM / PLM
    system_code       VARCHAR(64)  NOT NULL,        -- 该系统内的编码
    mapping_type      VARCHAR(16)  NOT NULL DEFAULT 'manual',  -- manual / auto_rule / merge_derived
    is_primary        BOOLEAN      NOT NULL DEFAULT false,
    status            VARCHAR(16)  NOT NULL DEFAULT 'active',  -- active / deprecated
    version           INT          NOT NULL DEFAULT 1,         -- 乐观锁
    checksum          VARCHAR(64)  NOT NULL,                   -- sha256(golden_id|system|code|version)
    created_by        VARCHAR(64)  NOT NULL,
    created_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),
    CONSTRAINT uq_mapping_system_code UNIQUE (system_name, system_code)
);
-- 每个金标在每个系统至多一条主映射
CREATE UNIQUE INDEX uq_mapping_primary
    ON cross_system_code_mappings (golden_record_id, system_name) WHERE is_primary;
```

映射实例（§1.2 的轴承）：

| golden_record_id | system_name | system_code | mapping_type | is_primary |
|---|---|---|---|---|
| GR-100045 | SAP-S4HANA | 10045678 | manual | true |
| GR-100045 | PLM | P-6204-2RS | auto_rule | true |
| GR-100045 | WMS | WH-B-0455 | manual | true |
| GR-100045 | SRM | SKF-6204-2RSH | manual | false |

**方案 3：映射 API（报文实例）：**

```
POST /api/mappings
{
  "golden_record_id": 100045,
  "system_name": "WMS",
  "system_code": "WH-B-0455",
  "is_primary": true
}
→ 201 { "id": 8812, "checksum": "9f2c…", "version": 1 }

冲突时（该 WMS 编码已绑到别的金标）：
→ 409 { "error": "MAPPING_CONFLICT",
        "detail": "WMS:WH-B-0455 已映射至 GR-100102",
        "existing": { "golden_record_id": 100102, "mapping_id": 8601 } }
```

并发安全：绑定走 `INSERT ... ON CONFLICT (system_name, system_code) DO NOTHING RETURNING id`，返回空即 409——双请求只有一个成功，数据库层保证。

**方案 4：回写策略。** 映射变更写 `MappingUpdated` 事件进 Outbox（携带 version + checksum）；下游按版本幂等消费。回写 SAP 走 BTP 通道并登记回执到 `external_system_logs`（该表已建模，本次补上写入点）。

**验收指标**：映射覆盖率 ≥ 95%（在用编码口径）；编码可解释率 100%；映射并发绑定双请求测试只有一个成功。

---

### 4.2 目标 A2：一物多码识别与合并

**现状**：`duplicate_detector.py` 只看名称、中文分词失效、不阻断。本节给出替换算法 + 完整合并流程。

#### （1）多字段加权相似度算法

```
similarity(A, B) = 0.35·name_sim + 0.30·spec_sim + 0.20·attr_sim
                 + 0.075·unit_match + 0.075·class_match
```

**name_sim**：归一化 → bigram Jaccard 与 pg_trgm 相似度取 max。归一化规则（按序执行）：

```
1. 全角→半角、大写→小写
2. × * x X → 统一为 x；φ Φ Ø → 统一为 phi
3. 剥离空格、/ \ - _ 等分隔符
4. GB/T → GB；级/CLASS/CL 等强度等级标记归一
```

**spec_sim**：从名称与描述中用正则抽取规格参数集，集合比对；**关键参数冲突直接判负**（一票否决）。

抽取正则实例（紧固件）：
```
螺纹规格:  [Mm](\d+(?:\.\d+)?)\s*[x×*]\s*(\d+(?:\.\d+)?)   → thread=M8, length=30
强度等级:  (\d{1,2}\.\d)\s*级?|CL\s*(\d{1,2}\.\d)          → grade=8.8
标准号:    GB/?T?\s*(\d{3,5})                              → std=GB5783
材质:      (304|316|45#|Q235|40Cr|65Mn)                    → material=304
```

#### （2）算法演算实例（三个真实候选）

```
A: 六角头螺栓 GB/T5783 M8×30 8.8级 镀锌
B: 螺栓 GB5783 M8*30 8.8
C: 内六角圆柱头螺钉 M8×30 12.9级

归一化:
A → 六角头螺栓gb5783m8x308.8镀锌      specs={std:GB5783, M8, 30, 8.8, 镀锌}
B → 螺栓gb5783m8x308.8               specs={std:GB5783, M8, 30, 8.8}
C → 内六角圆柱头螺钉m8x3012.9         specs={M8, 30, 12.9}

A vs B:
  name_sim  = max(bigram_jaccard=0.52, trgm=0.61) = 0.61
  spec_sim  = 交集{GB5783,M8,30,8.8}=4 / 并集{GB5783,M8,30,8.8,镀锌}=5 = 0.80，无冲突
  attr_sim  = 0.75（属性模板 8 项中 6 项一致）
  unit=1, class=1（同属小类"紧固件"）
  similarity = 0.35×0.61 + 0.30×0.80 + 0.20×0.75 + 0.075 + 0.075
             = 0.2135 + 0.24 + 0.15 + 0.15 = 0.7535  → 弱候选(0.60~0.85)，入候选池

A vs C:
  name_sim=0.38, spec 抽取发现 grade: 8.8 vs 12.9 → 关键参数冲突，直接判负
  similarity = 0.0（不进入候选）  ← 这就是"8.8 级螺栓绝不能和 12.9 级合并"的硬拦截
```

#### （3）判定与阻断策略

| 相似度 | 动作 |
|---|---|
| ≥ 0.85 | 强候选：**提交阻断**（409 + 候选清单），申请人须"确认非重复（填理由进审计）"或"转合并流程" |
| 0.60–0.85 | 弱候选：警告 + 记入候选池，Steward 工作台复核 |
| < 0.60 | 不提示 |

权重与阈值全部存 `governance_rules.expression`，可在规则页调整（目标 C 落地后零发版生效）。启用前**影子模式运行 2–4 周**：只记录不阻断，用人工标注样本校准阈值后再开阻断。

#### （4）性能保障（P95 < 5s，百万级记录）

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
ALTER TABLE golden_records ADD COLUMN name_normalized VARCHAR(512);
CREATE INDEX idx_gr_name_trgm ON golden_records USING gin (name_normalized gin_trgm_ops);

-- 候选召回：trgm 粗筛 top-200，再在应用层算加权精排
SELECT id, material_code, material_name,
       similarity(name_normalized, :q) AS trgm_sim
FROM golden_records
WHERE status = 'active' AND name_normalized % :q      -- % 为 trgm 相似操作符，走 GIN 索引
ORDER BY trgm_sim DESC
LIMIT 200;
```

#### （5）合并流程状态机与字段级决策

算法给候选、Steward 终判，不全自动合并：

```
候选生成 → Steward 复核 → 主记录选择 → 字段级决策 → 合并审批 → 执行
                                                              ├─ 生成新版本快照 change_type=merge
                                                              ├─ 旧编码写入映射表 mapping_type=merge_derived
                                                              ├─ 旧金标置 obsolete，不可再用于新申请
                                                              └─ 发布 MaterialMerged 事件
```

`merge_candidates` 表 DDL：

```sql
CREATE TABLE merge_candidates (
    id            BIGSERIAL PRIMARY KEY,
    gr_id_a       BIGINT NOT NULL REFERENCES golden_records(id),
    gr_id_b       BIGINT NOT NULL REFERENCES golden_records(id),
    similarity    NUMERIC(5,4) NOT NULL,
    evidence      JSONB  NOT NULL,     -- 各维度得分 + 抽取的规格参数
    status        VARCHAR(16) NOT NULL DEFAULT 'pending',
                  -- pending / confirmed / rejected / merged
    decided_by    VARCHAR(64),
    decided_at    TIMESTAMPTZ,
    decision_note TEXT,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT uq_candidate_pair UNIQUE (gr_id_a, gr_id_b),
    CONSTRAINT chk_no_self CHECK (gr_id_a < gr_id_b)   -- 配对只存一次
);
```

**字段级决策实例**（螺栓 A 与 B 合并，A 为主记录）：

| 字段 | A 值 | B 值 | 决策 | 决策人 |
|---|---|---|---|---|
| material_name | 六角头螺栓 GB/T5783 M8×30 8.8级 镀锌 | 螺栓 GB5783 M8*30 8.8 | 取 A（符合命名标准） | Steward |
| purchasing_group | 空 | P03 | 取 B（A 缺失） | Steward |
| safety_stock | 5000 | 8000 | 人工修订 10000（合并需求） | 品类 Owner |
| inspection_plan | 空 | IQC-FASTENER-01 | 取 B | Steward |

决策快照（含每字段取值来源）写入 `golden_record_versions.details`，永久可回溯。

**验收指标**：重码率下降 ≥ 50%；预检覆盖率 100%；中文标注测试集（≥ 200 对）召回 ≥ 90%、误报 ≤ 15%；合并全流程（候选→回写）审计链完整。

---

### 4.3 目标 B：关键字段完整性门禁

**现状**：`is_required` + 提交/审批两道门禁，质量分已落地；缺条件必填、格式规则、发布端校验。

**方案 1：属性模板扩展（DDL）：**

```sql
ALTER TABLE attribute_templates
    ADD COLUMN required_condition JSONB,   -- {"when":{"material_type":"raw"},"then":"required"}
    ADD COLUMN format_rule        JSONB,   -- {"regex":"^\\d{4}-\\d{2}$","example":"2026-08"}
    ADD COLUMN value_domain       JSONB,   -- {"type":"range","min":0,"max":100} 或 {"type":"enum","values":[...]}
    ADD COLUMN unit_group         VARCHAR(32),  -- length: mm/cm/m 自动换算校验
    ADD COLUMN owner              VARCHAR(64),  -- 字段责任人
    ADD COLUMN source_system      VARCHAR(32),  -- 字段权威来源系统
    ADD COLUMN lifecycle_status   VARCHAR(16) DEFAULT 'active';
```

**方案 2：真实字段规则配置实例（紧固件小类，可直接入库）：**

```json
[
  {"field_name": "std_no",       "field_label": "执行标准",  "required_condition": {"when": {"classification": "10302"}, "then": "required"},
   "format_rule": {"regex": "^(GB|ISO|DIN|JB)/?T?\\s*\\d{3,5}(-\\d{4})?$", "example": "GB/T 5783-2016"}},
  {"field_name": "thread_spec",  "field_label": "螺纹规格",
   "format_rule": {"regex": "^M\\d+(\\.\\d+)?x\\d+(\\.\\d+)?$", "example": "M8x30"}},
  {"field_name": "strength_grade","field_label": "强度等级",
   "value_domain": {"type": "enum", "values": ["4.8","8.8","10.9","12.9","A2-70"]}},
  {"field_name": "surface_treatment", "field_label": "表面处理",
   "value_domain": {"type": "enum", "values": ["镀锌","镀镍","发黑","达克罗","本色"]}},
  {"field_name": "batch_managed","field_label": "批次管理", "field_type": "boolean",
   "required_condition": {"when": {"material_type": "raw"}, "then": "required"}},
  {"field_name": "shelf_life_months", "field_label": "保质期(月)",
   "value_domain": {"type": "range", "min": 1, "max": 120},
   "required_condition": {"when": {"field": "batch_managed", "eq": true}, "then": "required"}}
]
```

注意最后一条：`batch_managed=true` 时保质期才必填——这就是条件必填的实际用法。

**方案 3：五个校验执行点**（修复"发布端不重跑"缺口）：

| 执行点 | 触发 | 强度 |
|---|---|---|
| 1. 草稿保存 | 前端保存 | warning 展示，不阻断 |
| 2. 提交 | submit API | 强校验，blocking 即 400（已有） |
| 3. 审批 | admin-approve | 门禁重跑（已有，防快照过期） |
| 4. **发布** | publish API | **全量校验（新增）**——字段标准在审批后可能升版，发布前必须按当前版本再过一遍 |
| 5. 周期巡检 | 批量扫描 | 接入 §4.4 存量扫描，标准升版后回放存量 |

**方案 4：按物料类型差异化模板。** 五类 `MaterialType` 各自定义关键字段集，附录 A 给出紧固件/轴承/电机三张完整模板实例。

**验收指标**：增量关键字段完整率 ≥ 98%；缺失字段的发布请求拦截率 100%；条件必填规则测试覆盖五类物料。

---

### 4.4 目标 C：规则引擎升级（JSON DSL）

**现状**：`governance_rules` 只存元策略，逻辑硬编码在 `MaterialValidator`。

**方案：七类规则 + 双执行模式。** `governance_rules` 增加 `rule_type`、`expression JSONB`、`execution_mode`：

```sql
ALTER TABLE governance_rules
    ADD COLUMN rule_type       VARCHAR(16) NOT NULL DEFAULT 'custom',
    ADD COLUMN expression      JSONB,
    ADD COLUMN execution_mode  VARCHAR(16)[] NOT NULL DEFAULT '{realtime}';
```

七类规则 + 生产实例：

| rule_type | 语义 | 实例（expression） |
|---|---|---|
| required | 必填/条件必填 | `{"field":"purchasing_group","when":{"material_type":"raw"}}` |
| uniqueness | 唯一性（支持组合键） | `{"fields":["manufacturer","manufacturer_part_no"],"scope":"active"}` |
| format | 正则/长度 | `{"field":"material_name","regex":"^[\\u4e00-\\u9fa5A-Za-z0-9（）()xφ\\-]{5,200}$"}` |
| domain | 值域/枚举/引用字典 | `{"field":"unit","ref_dict":"uom_standard"}` |
| consistency | 跨字段一致 | `{"expr":"price_with_tax >= price_without_tax","message":"含税价不得小于不含税价"}` |
| timeliness | 时效性 | `{"field":"expiry_date","must_be":">today"}` |
| reference | 引用完整 | `{"field":"bom_child_code","ref":"golden_records.material_code","status":"active"}` |

`manufacturer + manufacturer_part_no` 组合唯一是备件域最实用的一条：同一原厂件号永远只对应一个 Golden ID，从源头掐断备件重码。

执行模式：`realtime`（提交/审批/发布同步跑）+ `batch`（存量扫描跑）。规则与代码解耦——调阈值/级别/开关零发版，新增规则类型才需要开发。`MaterialValidator` 重构为"DSL 解释器 + 规则注册表"，现有 8 条硬编码规则迁移为 DSL 初始数据（init_db.py 种子）。

标准层版本管理：分类/字段/编码/术语四类标准均带 `version + effective_from/to`；审计 details 已存 `rule_version`（当前 `mdg-p0-v1`），历史校验结果可按当时版本回放复现。

**验收指标**：新增标准覆盖率 ≥ 90%；规则调整配置生效 < 1 分钟；七类规则各有 ≥ 1 条生产实例 + 对应测试。

---

### 4.5 目标 D：存量清洗 + 增量防错双循环

**现状**：增量闭环已通，存量清洗完全缺失。假设盘点基线：物料 120,000 条（SAP 侧），初检重码率 4.2%、关键字段完整率 81%。

**方案 1：扫描作业表（DDL）：**

```sql
CREATE TABLE quality_scan_jobs (
    id            BIGSERIAL PRIMARY KEY,
    job_name      VARCHAR(128) NOT NULL,
    scope         JSONB NOT NULL,          -- {"classification":"10302","status":"active"}
    rule_version  VARCHAR(32) NOT NULL,    -- 按哪个规则版本扫
    data_version  VARCHAR(32) NOT NULL,    -- 数据快照标识，幂等键的一部分
    status        VARCHAR(16) NOT NULL DEFAULT 'pending',  -- pending/running/done/failed
    total_records INT, scanned_records INT DEFAULT 0,
    issues_found  INT DEFAULT 0,
    started_at TIMESTAMPTZ, finished_at TIMESTAMPTZ,
    created_by VARCHAR(64) NOT NULL,
    CONSTRAINT uq_scan_idem UNIQUE (scope, rule_version, data_version)  -- 幂等
);

CREATE TABLE quality_issues (
    id            BIGSERIAL PRIMARY KEY,
    job_id        BIGINT NOT NULL REFERENCES quality_scan_jobs(id),
    golden_record_id BIGINT NOT NULL REFERENCES golden_records(id),
    rule_key      VARCHAR(64) NOT NULL,
    field_name    VARCHAR(64),
    severity      VARCHAR(16) NOT NULL,
    current_value TEXT,
    suggestion    TEXT,                  -- 修复建议（L1/L2 证据）
    status        VARCHAR(16) NOT NULL DEFAULT 'open',   -- open/ticketed/fixed/ignored
    ticket_id     BIGINT
);

CREATE TABLE data_quality_tickets (
    id            BIGSERIAL PRIMARY KEY,
    title         VARCHAR(256) NOT NULL,
    issue_ids     BIGINT[]  NOT NULL,      -- 关联问题
    assignee      VARCHAR(64) NOT NULL,    -- 责任人
    sla_due_at    TIMESTAMPTZ NOT NULL,
    status        VARCHAR(16) NOT NULL DEFAULT 'open',
                  -- open/assigned/fixing/verifying/closed
    escalate_level INT NOT NULL DEFAULT 0, -- 0 无 1 部门负责人 2 治理委员会
    closed_at     TIMESTAMPTZ,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**方案 2：工单 SLA 升级链（实例）：**

```
工单创建（assignee=物料组 Steward，SLA=3 天）
  ├─ 3 天未闭环 → escalate_level=1，通知部门负责人
  └─ 7 天未闭环 → escalate_level=2，进治理委员会周会议题
```

**方案 3：存量清洗实操六步法（附录 B 有完整 SQL）：**

1. 基线扫描：全量跑 batch 规则，出《问题热力图》——按"问题类型 × 分类"透视
2. 排序：按"业务影响 × 问题量"取 Top 3 问题域（典型结果：制造商料号缺失、计量单位非标、重码候选）
3. 首批试点：选 1 个小类（如紧固件，约 3,000 条）做全闭环验证：扫描 → 工单 → 修复 → 回写 → 复扫
4. 分批推广：每批 5,000–10,000 条，修复前后跑规则评分对比
5. 回写落地：SAP 走 BTP 批量回写（每批 ≤ 500 条 + 回执登记）；其余系统走适配器
6. 效果入驾驶舱：重码率、完整率、一致率三条曲线按月出数

**方案 4：增量防错补强：**
- 重复预检改阻断式（§4.2 强候选 409）
- `publish_sync_tasks` 增加后台 worker：指数退避 `next_retry_at = now() + (2^attempt) minutes`（max_attempts=5 字段已存在），5 次失败进死信（status=dead），保留现有人工重试端点兜底
- BTP/OM 从 Mock 切真实调用，`external_system_logs` 补写入点（request/response/duration_ms 字段已建好）

**验收指标**：Top 问题域闭环率 ≥ 85%；跨系统一致率 ≥ 95%；MTTR 逐月下降；同步自动恢复率 ≥ 95%。

---

### 4.6 目标 E：语义与指标治理

**现状**：无术语词典、无指标治理。

**方案 1：两张表（DDL 节选）：**

```sql
CREATE TABLE business_terms (
    id BIGSERIAL PRIMARY KEY,
    term_name     VARCHAR(128) NOT NULL,
    synonyms      VARCHAR(128)[],              -- 同义词数组
    definition    TEXT NOT NULL,
    domain        VARCHAR(64),                 -- 关联数据域
    related_fields JSONB,                      -- [{"table":"golden_records","field":"unit"}]
    owner VARCHAR(64), version INT NOT NULL DEFAULT 1,
    status VARCHAR(16) DEFAULT 'active',
    UNIQUE (term_name, version)
);

CREATE TABLE metric_definitions (
    id BIGSERIAL PRIMARY KEY,
    metric_name   VARCHAR(128) NOT NULL,
    business_caliber TEXT NOT NULL,            -- 业务口径
    formula       TEXT NOT NULL,               -- 计算逻辑（SQL 伪码）
    dimensions    VARCHAR(64)[],               -- 可用维度
    granularity   VARCHAR(32),                 -- 日/周/月
    source_fields JSONB NOT NULL,              -- 来源字段血缘引用
    owner VARCHAR(64), version INT NOT NULL DEFAULT 1,
    effective_from DATE NOT NULL, effective_to DATE,
    UNIQUE (metric_name, version)
);
```

**方案 2："准时率"消歧实例**——同一个词两个口径，必须拆成两个指标：

| metric_name | version | business_caliber | formula |
|---|---|---|---|
| 采购准时交付率 | 1 | 按采购订单承诺日期判定 | `sum(case when actual_delivery_date <= promised_date then 1 else 0 end) / count(po_lines)` |
| 生产完工准时率 | 1 | 按工单计划完工日期判定 | `sum(case when actual_finish <= planned_finish then 1 else 0 end) / count(work_orders)` |

BI 报表引用指标必须绑定 `(metric_name, version)`；口径变更必须升版并通知订阅方，禁止原地改定义。

**方案 3：知识底座输出**（供 AI 问数检索的 JSON 实例）：

```json
{
  "term": "准时率",
  "disambiguation": [
    {"metric": "采购准时交付率", "version": 1, "owner": "采购部",
     "fields": ["po_line.promised_date", "po_line.actual_delivery_date"],
     "lineage": "golden_records → BTP → S/4HANA EKET"},
    {"metric": "生产完工准时率", "version": 1, "owner": "生产部", "...": "..."}
  ]
}
```

**方案 4：问数护栏。** AI 问数请求 → 术语消歧 → 口径绑定 → 生成查询 → 回答附治理证据（指标版本 + 血缘引用）；绑不上证据的回答标记"未治理"。示例见 §5.4。

**验收指标**：Top 50 核心指标 100% 版本化定义；AI 问数证据绑定率 ≥ 95%；BI 口径冲突数逐月下降。

---

## 5. AI / Agent 嵌入方案（含实际输出示例）

### 5.1 核心定位：AI 增强型确定性治理

主数据是零容错场景。核心治理逻辑（标准校验、质量规则、归并执行、编码映射）必须由确定性规则引擎承载；LLM 只在建议层工作：语义判断、候选召回、冲突提示、证据组织、自然语言交互。抽掉 LLM，系统照常运转。

价值主张：**AI 提升人的判断效率，不替代人的治理责任。** AI 也会错，系统的价值在于让错误"可见、可拦、可追"。

### 5.2 证据分级（替代 LLM 置信度）

LLM 自报置信度校准差（完全错误也给 95%），不可用。改用证据类型分级：

| 级别 | 证据类型 | 实例 | 系统行为 |
|---|---|---|---|
| L1 | 规则确定性命中（正则/映射表/字典） | "计量单位 '个' 在标准字典 UOM-STD-V3 中" | 可一键采纳，不自动写库 |
| L2 | 数据统计推断（带样本） | "该分类下 97% 物料采购组为 P01（样本 312 条）" | 建议 + 展示样本依据 |
| L3 | 语义推断（LLM） | "名称语义相近，疑似同一物料" | 永远标注"需人工确认"，只出候选 |

### 5.3 Agent 角色与边界

| Agent | 职责 | 证据级别 | 自动执行边界 |
|---|---|---|---|
| 标准审查 Agent | 命名/分类/编码合规检查 | L1 | 只出报告 |
| 重码识别 Agent | 语义增强候选召回（补充 §4.2 规则算法） | L2/L3 | 只入候选池，合并必走人工审批 |
| 字段补全 Agent | 基于上下文与历史样本建议字段值 | L2/L3 | 建议需逐条采纳 |
| 根因分析 Agent | 解释"为何不一致"，定位源系统与流程环节 | L2 | 只出分析 |
| 问数护栏 Agent | 术语消歧、口径绑定、证据引用 | L1/L2 | 无证据绑定则拒答或标注 |

**统一红线**：写入金标库 / 分发 / 归并一律人工确认；自动仅限无副作用环节（扫描、建议、报告）。

### 5.4 实际输出示例

**字段补全 Agent 输出（JSON）：**

```json
{
  "trace_id": "tr-20260827-8f31a2",
  "target": {"golden_record_id": 100045, "field": "purchasing_group"},
  "suggestion": "P01",
  "evidence_level": "L2",
  "evidence": {
    "basis": "同分类 10302(紧固件) 下 purchasing_group 分布",
    "sample_size": 312,
    "distribution": {"P01": 302, "P03": 7, "null": 3}
  },
  "requires_human_confirm": true,
  "model": {"name": "qwen-max", "version": "2026-06"}
}
```

**问数护栏 Agent 对话实例：**

```
用户: 上个月准时率多少？
Agent: "准时率"有两个已治理口径，请确认：
       [1] 采购准时交付率 v1（采购部，按 PO 承诺日期）
       [2] 生产完工准时率 v1（生产部，按工单计划完工）
用户: 1
Agent: 2026-07 采购准时交付率 = 92.4%（口径 v1，样本 4,817 行 PO 行项目）
       证据: 指标定义 metric_definitions#PO-OTD:v1；
             血缘 golden_records → BTP → S/4HANA EKET；
             trace tr-20260827-9c04e1
```

**根因分析 Agent 报告实例（§1.2 轴承单位不一致）：**

```
结论: WMS 单位 PC 未纳入 UOM 标准字典映射（PC/个/件 应为同组单位）。
证据链: L1 - uom_standard 字典查无 'PC'；
       L2 - WMS 侧 3,412 条记录中 2,901 条单位为 PC（85%）；
定位: 环节=WMS 接口初始化(2024-03)；建议动作=字典补 PC→个 映射 + 存量批量换算。
```

### 5.5 工程机制

- **LLM 网关**：超时 15s、重试 2 次、熔断降级（LLM 挂了规则引擎独立运行）、token 成本上限
- **agent_trace 表**（DDL）：

```sql
CREATE TABLE agent_traces (
    id            BIGSERIAL PRIMARY KEY,
    trace_id      VARCHAR(64) NOT NULL,          -- 贯穿全链
    agent_name    VARCHAR(64) NOT NULL,
    model_name    VARCHAR(64), model_version VARCHAR(32),
    input_digest  TEXT NOT NULL,                 -- 输入摘要（非全量，防敏感泄露）
    evidence_refs JSONB NOT NULL,                -- 引用的规则/字典/样本
    evidence_level VARCHAR(4) NOT NULL,          -- L1/L2/L3
    output        JSONB NOT NULL,
    human_decision VARCHAR(16),                  -- accepted/rejected/modified/pending
    decided_by    VARCHAR(64), decided_at TIMESTAMPTZ,
    tokens_used   INT, latency_ms INT,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_agent_traces_trace ON agent_traces (trace_id);
```

- **成功标准**：不考核"发现率 100%"，考核**每个缺陷都被闭环处理**——自动建议、人工裁决或工单跟踪，无一静默漏过。

### 5.6 行业参照（事实核查后引用）

- 用友 2026-03-16 发布 BIP 数据治理 Agents 协作平台，首批 16 个专业智能体、五大类、白盒可追溯（官网可核实）；其"自动化 85%、效率提升 3 倍"为厂商自报指标，引用须注明。
- 魏桥创业集团（2025 年营收 5915 亿元、18 个基地、11 万员工，政府发布会与央媒口径）员工两个月自建 800+ AI 助理——**该批助理基于钉钉平台，与其 MDM 项目无关，引用时不得混用**。
- 魏桥 × 中翰"AI 原生 MDM"合作属实但仅厂商单方来源，"国内首款"为营销口径，不作为论证依据。

---

## 6. 平台能力蓝图与模块映射

| 新增模块 | 对应目标 | 关键构成 | 复用现状 |
|---|---|---|---|
| 主数据标准中心 | C | 编码/字段/术语标准 + 版本管理 | 分类树、属性模板、code_rules 升级 |
| 统一编码与映射中心 | A | Golden ID、cross_system_code_mappings、回写策略 | 原子编码生成 |
| 规则与质量引擎 | C | 七类规则 DSL、realtime/batch 双模式 | MaterialValidator 重构 |
| 重复识别与合并中心 | A2 | 多字段相似度、merge_candidates、字段级决策工作台 | DuplicateDetector 重写 |
| 主数据工单中心 | D | 扫描作业/问题/工单三表、SLA 升级 | 审计与通知复用 |
| 语义与指标治理中心 | E | business_terms、metric_definitions、知识底座 | metadata_governance 扩展 |
| 治理运营驾驶舱 | 全局 | 质量分、问题趋势、MTTR、SLA、组织绩效 | dashboard + audit_logs 回放 |
| AI 治理助手 | 5 | 五类 Agent、LLM 网关、agent_trace | 审计 details 为证据底座 |

---

## 7. 分阶段实施计划

### 7.1 总体阶段（64–101 人天基线，团队并行可压缩至 6–9 个月）

| 阶段 | 周期 | 范围 | 关键交付物 | 验收门禁 |
|---|---|---|---|---|
| Phase 0 基线评估 | 0–1 月 | 数据盘点、系统清单、问题热力图、治理章程 | 盘点报告、映射清单、章程 | 关键数据域覆盖 ≥ 80% |
| Phase 1 标准与模型 | 1–3 月 | §4.1 + §4.4 标准层 | 编码规则版本化、字段标准、分类标准、映射表上线 | 新增标准覆盖率 ≥ 90%；编码四态可用 |
| Phase 2 核心治理能力 | 3–6 月 | §4.2 + §4.3 + §4.4 规则引擎 | 阻断式重复识别、合并工作台、规则 DSL 引擎、映射 API、金标生命周期前端 | 中文测试集召回 ≥ 90%；重码率降 ≥ 50%；完整率升 ≥ 60% |
| Phase 3 存量治理专项 | 6–9 月 | §4.5 + 真实集成 | 批量扫描、工单、回写适配器、BTP/OM 真实接入、worker+退避+死信、用户库入库+SSO | Top 问题域闭环 ≥ 85%；一致率 ≥ 95%；同步自动恢复 ≥ 95% |
| Phase 4 智能治理与产品化 | 9–12 月 | §4.6 + §5 | 术语/指标治理、知识底座、问数护栏、五类 Agent、PostgreSQL 迁移+Alembic+Docker/K8s | 修复人效提升 2–3 倍；问数证据绑定 ≥ 95% |

### 7.2 Phase 2 任务级分解（实例，可直接排期）

| # | 任务 | 涉及文件（现状） | 人天 |
|---|---|---|---|
| 1 | 名称归一化 + bigram/trgm 相似度函数 + 单测 | 重写 `duplicate_detector.py` | 4 |
| 2 | 规格参数抽取器（紧固件/轴承/电机三族正则库） | 新增 `services/spec_extractor.py` | 3 |
| 3 | pg_trgm 索引 + 候选召回 SQL + Alembic 迁移 | `models.py`、迁移脚本 | 2 |
| 4 | merge_candidates 表 + 候选池 API | `models.py`、`api/`、`crud.py` | 4 |
| 5 | 合并执行器（字段级决策 + 快照 + 映射派生 + 事件） | `crud.py:252-434` 扩展 | 5 |
| 6 | 合并工作台前端（候选列表/对比视图/字段决策） | `src/pages/` 新增 | 5 |
| 7 | 阻断式预检接入 submit（409 + 审计） | `api/applications.py:244` | 2 |
| 8 | 影子模式开关 + 标注样本采集脚本 | `duplicate_detector.py` | 2 |
| 9 | 测试：相似度算法 200 对标注集 + 合并流程 E2E | `backend/tests/` | 4 |

小计约 31 人天，与 ROADMAP 的 P1 段（17–27 人天，不含前端工作台）口径一致。

### 7.3 90 天落地计划（逐周）

| 周 | 动作 | 产出 |
|---|---|---|
| W1–2 | 试点范围圈定（1 个事业部 + 紧固件小类 ~3,000 条）；数据导出盘点 | 基线报告（重码率/完整率/不一致清单） |
| W3–4 | 字段标准与编码规则评审定稿；分类树与属性模板入库 | 标准 v1.0、模板入库 |
| W5–6 | 名称归一化 + 相似度算法开发；pg_trgm 索引上线（迁 PostgreSQL） | 算法单测绿 |
| W7–8 | 影子模式跑存量 3,000 条，出候选清单；Steward 人工标注 ≥ 200 对 | 阈值校准报告 |
| W9–10 | 阻断式预检上线；合并工作台 MVP；首批合并 50 组 | 合并快照 + 映射派生可审计 |
| W11 | 质量工单上线；Top 3 问题开工单分派 | 工单 SLA 运转 |
| W12 | 驾驶舱上线（重码率/完整率/MTTR）；复盘会 | 90 天成效报告，决策推广范围 |

### 7.4 实施纪律（强制执行）

- Ralph Loop：最小可验证闭环，每轮只扩展一个闭环节点——先让链路变长，再让节点变厚
- TDD：RED→GREEN→REFACTOR；测试金字塔 单元 ~70% / 集成 ~20% / API-E2E ~10%
- 每个需求至少一个独立提交；失败路径与成功路径同等重要（BTP 失败、同步失败、越权、编码冲突都要可见/可重试/可审计）
- 上线门禁清单：无 SAP 直连配置、OAuth Scope/Token 最小权限、Agent 工具白名单、失败可重试、requestId 全链串联

---

## 8. KPI 体系（公式 + 取数 SQL）

| KPI | 公式 | 目标 | 取数 SQL（实例） |
|---|---|---|---|
| 主数据唯一率 | 1 − 重复组数/物料总数 | ≥ 99% | `SELECT 1 - count(*) FILTER (WHERE status='merged')::numeric / count(*) FROM merge_candidates;` |
| 重码发现率 | 系统检出 / (系统检出+人工新报) | ≥ 90% | merge_candidates 按来源统计 |
| 重复闭环率 | status IN (merged,rejected) / 总候选 | ≥ 95% | `SELECT count(*) FILTER (WHERE status IN ('merged','rejected'))::numeric/count(*) FROM merge_candidates;` |
| 关键字段完整率 | 完整记录/总记录 | ≥ 98% | 见附录 B 扫描 SQL |
| 映射覆盖率 | 已映射在用编码/在用编码总数 | ≥ 95% | `SELECT count(DISTINCT system_code) FROM cross_system_code_mappings WHERE status='active';` 对比源系统导出 |
| 跨系统一致率 | 一致记录/比对记录（按字段） | ≥ 95% | 对账作业逐字段比对 |
| 问题 MTTR | Σ(closed_at−created_at)/关闭工单数 | 逐月降 | `SELECT avg(closed_at - created_at) FROM data_quality_tickets WHERE status='closed' AND closed_at >= date_trunc('month', now());` |
| 同步自动恢复率 | 自动成功/失败任务总数 | ≥ 95% | publish_sync_tasks 按 attempt_count 统计 |
| AI 建议采纳率 | accepted / 总建议 | 观测 | `SELECT count(*) FILTER (WHERE human_decision='accepted')::numeric/count(*) FROM agent_traces;` |
| 问数证据绑定率 | 附证据回答/总回答 | ≥ 95% | agent_traces.evidence_refs 非空占比 |
| BI 口径冲突数 | 同名活跃指标版本数 | 逐月降 | `SELECT metric_name, count(*) FROM metric_definitions WHERE status='active' GROUP BY 1 HAVING count(*)>1;` |

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 存量质量比预期差，清洗量爆炸 | Phase 3 延期 | Phase 0 盘点即量化；按业务影响分批，不追求一次清完 |
| 中文重复识别误报高，业务失去信任 | 阻断被绕过 | L1/L2 规则为主、L3 语义只作补充候选；影子模式跑 2–4 周校准阈值后再开阻断 |
| 各事业部标准不统一、归并权责争议 | 合并停滞 | 工单 SLA 升级链（3 天部门负责人/7 天治理委员会）；跨组织归并由治理委员会裁决 |
| 源系统回写接口受限 | 修复落不了地 | 适配器逐系统认证；不可回写的降级为"映射表 + 定期对账" |
| LLM 输出不稳定 | 建议质量波动 | LLM 网关熔断降级；核心逻辑不依赖 LLM；agent_trace 全量留痕可回放 |
| SQLite → PostgreSQL 迁移风险 | 数据丢失 | Alembic 迁移 + 迁移前后校验和比对；pg_trgm 扩展提前验证 |

---

## 附录 A：字段标准模板实例（三张，可直接入库）

### A.1 紧固件（小类 10302）

| 字段 | 标签 | 必填 | 规则 |
|---|---|---|---|
| std_no | 执行标准 | 是 | 正则 `^(GB\|ISO\|DIN\|JB)/?T?\s*\d{3,5}(-\d{4})?$` |
| thread_spec | 螺纹规格 | 是 | 正则 `^M\d+(\.\d+)?x\d+(\.\d+)?$` |
| strength_grade | 强度等级 | 是 | 枚举 {4.8, 8.8, 10.9, 12.9, A2-70} |
| material | 材质 | 是 | 枚举 {45#, Q235, 40Cr, 304, 316, 65Mn} |
| surface_treatment | 表面处理 | 是 | 枚举 {镀锌, 镀镍, 发黑, 达克罗, 本色} |
| batch_managed | 批次管理 | 条件（原材料必填） | boolean |
| shelf_life_months | 保质期（月） | 条件（batch_managed=true） | 区间 1–120 |

### A.2 轴承（小类 10204）

| 字段 | 标签 | 必填 | 规则 |
|---|---|---|---|
| bearing_model | 轴承型号 | 是 | 正则 `^\d{4}(-2RS\|-ZZ\|-2Z)?$`，示例 6204-2RS |
| bearing_type | 轴承类型 | 是 | 枚举 {深沟球, 角接触, 圆锥滚子, 推力球, 调心滚子} |
| inner_diameter | 内径（mm) | 是 | 区间 3–500；且须与型号解析值一致（跨字段 consistency 规则实例） |
| outer_diameter | 外径（mm) | 是 | 区间 10–800 |
| width | 宽度（mm) | 是 | 区间 4–300 |
| manufacturer | 制造商 | 是 | 引用字典 manufacturer_dict |
| manufacturer_part_no | 制造商料号 | 是 | 与 manufacturer 组合作 uniqueness 规则 |

`inner_diameter` 与型号一致性规则实例：6204 → 内径 = 04×5 = 20mm，填 25 即 blocking。这条规则在现场能拦掉大量抄错型号的数据。

### A.3 三相异步电机（小类 30701）

| 字段 | 标签 | 必填 | 规则 |
|---|---|---|---|
| motor_model | 电机型号 | 是 | 正则 `^YE[2-5]-\d{2,3}[MLST]-\d+$`，示例 YE3-160M-4 |
| rated_power_kw | 额定功率（kW) | 是 | 枚举标准功率序列 {0.75,1.1,1.5,…,11,15,18.5,…,315} |
| poles | 极数 | 是 | 枚举 {2,4,6,8}；且须与型号尾段一致 |
| efficiency_grade | 能效等级 | 是 | 枚举 {IE2, IE3, IE4, IE5}；YE3 必须 ≥ IE3（consistency） |
| ip_rating | 防护等级 | 是 | 正则 `^IP[2-6]\d$` |
| voltage_v | 电压（V) | 是 | 枚举 {220, 380, 660, 3000, 6000, 10000} |

---

## 附录 B：存量清洗实操手册（SQL 级）

### B.1 基线扫描——重码候选粗筛（影子模式）

```sql
-- 找名称 trgm 相似度 ≥ 0.5 的同小类物料对（PostgreSQL）
SELECT a.id AS id_a, b.id AS id_b,
       a.material_name, b.material_name,
       similarity(a.name_normalized, b.name_normalized) AS sim
FROM golden_records a
JOIN golden_records b
  ON a.classification_id = b.classification_id
 AND a.id < b.id
 AND a.name_normalized % b.name_normalized
WHERE a.status = 'active' AND b.status = 'active'
  AND similarity(a.name_normalized, b.name_normalized) >= 0.5
ORDER BY sim DESC;
```

### B.2 基线扫描——关键字段缺失

```sql
-- 紧固件小类关键字段完整率（attribute_values 为 JSONB）
SELECT
  count(*) AS total,
  count(*) FILTER (WHERE attribute_values->>'std_no' IS NOT NULL
                        AND attribute_values->>'std_no' <> '')        AS has_std,
  count(*) FILTER (WHERE attribute_values->>'strength_grade' IS NOT NULL
                        AND attribute_values->>'strength_grade' <> '') AS has_grade,
  round(100.0 * count(*) FILTER (WHERE attribute_values->>'std_no' <> ''
                                    AND attribute_values->>'strength_grade' <> '')
        / count(*), 1) AS complete_pct
FROM golden_records
WHERE classification_id = 10302 AND status = 'active';
```

### B.3 跨系统一致性对账（单位字段实例）

```sql
SELECT g.material_code, g.attribute_values->>'unit' AS golden_unit,
       m.system_name, m.system_code, e.unit_in_source
FROM golden_records g
JOIN cross_system_code_mappings m ON m.golden_record_id = g.id
JOIN staging_source_extract e                          -- 源系统每日抽取的落地表
  ON e.system_name = m.system_name AND e.system_code = m.system_code
WHERE e.unit_in_source IS DISTINCT FROM g.attribute_values->>'unit'
  AND NOT EXISTS (SELECT 1 FROM uom_conversion u        -- 排除可换算单位
                  WHERE u.from_unit = e.unit_in_source
                    AND u.to_unit   = g.attribute_values->>'unit');
```

### B.4 回写 SAP 实操

```
通道: BTP Integration Suite → S/4HANA OData API_PRODUCT_SRV (PATCH)
批量: 每批 ≤ 500 条，批间隔 2s，失败条目单独重试
回执: 每条写 external_system_logs (request/response/duration_ms)
幂等: 请求头带 Idempotency-Key = sha256(golden_id + field + value + version)
对账: T+1 跑 B.3 复核，差异进对账工单
```

---

## 附录 C：编码规则实例集

| 规则名 | pattern | 实例 | 适用 |
|---|---|---|---|
| 原材料-紧固件 | `RM-{大类}{小类}-{流水:5}` | RM-10302-00123 | 分类 10302 |
| 备件-电气 | `SP-{大类}{小类}-{流水:5}` | SP-30701-00008 | 分类 30701 |
| 成品 | `FG-{大类}-{年份}-{流水:4}` | FG-21-2026-0158 | 年度流水 |
| 通用兜底 | `{大类}-{小类}-{流水:5}` | 10-302-00123 | 无专项规则时 |

编码四态流转实例：申请人提交 → `simulate` 预演显示 "RM-10302-00124"（不占号）→ 进入审批 → `reserve` 占号防重 → 发布 `confirm` 生效 / 驳回 `release` 回收。

---

## 附录 D：术语词典实例（节选）

| term_name | synonyms | definition | owner |
|---|---|---|---|
| 金标数据 | Golden Record, 黄金记录, 主记录 | 经治理流程确认的唯一权威物料记录 | 数据治理委员会 |
| 一物多码 | 重码, 重复物料 | 同一实物对应多个物料编码的现象 | 数据治理委员会 |
| 准时率 | OTD, 准时交付 | **歧义词**：须消歧为"采购准时交付率"或"生产完工准时率" | 数据治理委员会 |
| 批次管理 | Batch Management | 物料是否按批次追踪库存与质量的标识 | 质量部 |

---

## 附录 E：现状代码与本文档的映射

- 数据模型：`backend/app/models.py`（10 张表，models.py:73–360）
- 质量校验：`backend/app/services/material_validator.py`（8 条默认规则 :12-21）
- 重复预检：`backend/app/services/duplicate_detector.py`（阈值 0.8/top-5 :85，中文失效）
- 编码生成：`backend/app/services/code_generator.py` + `backend/app/crud.py:177`（原子 UPDATE...RETURNING）
- 金标版本：`backend/app/crud.py:252-434`（修订/失效/回滚）
- 发布同步：`backend/app/models.py:337`（publish_sync_tasks，max_attempts=5）
- BTP Mock：`backend/app/services/btp_mock.py`；OM 半 Mock：`backend/app/services/openmetadata_sync.py:65-73`
- 测试：`backend/tests/`（10 个文件，~170 用例）

## 附录 F：参考资料

- `D:\AI\14 - 数据治理\RalphLoop_物料主数据治理_PRD.md`（业务背景、非功能指标、角色定义）
- `D:\AI\14 - 数据治理\OpenMetadata_物料主数据自研服务集成设计.md`（三层分工、四条铁律、合并流程、同步策略）
- `D:\AI\14 - 数据治理\OpenMetadata_本地部署可行性研究.md`（部署资源基线）
- `D:\AI\14 - 数据治理\AI数据治理案例_事实核查.md`（行业案例及引用口径）
- `D:\AI\14 - 数据治理\docs\superpowers\specs\2026-08-25-ai-native-dg-demo-design.md`（AI 增强定位、证据分级、Agent 工程机制）
- `ROADMAP.md`（P0–P3 差距与工作量估算）、`TC.md`（TDD 用例与五大治理目标）
