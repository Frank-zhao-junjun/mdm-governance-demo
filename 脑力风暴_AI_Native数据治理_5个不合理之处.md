# 脑力风暴：《AI-native 数据治理 Demo 设计文档》的 5 个不合理之处

> 日期：2026-08-25
> 批判对象：`docs/superpowers/specs/2026-08-25-ai-native-dg-demo-design.md`（以下简称"Spec"）
> 立场：不是否定 AI 在数据治理中的价值，而是逐条审视 Spec 中被当作既定结论、但经不起推敲的前提

---

## 不合理之一："AI-native"名不副实——Spec 第 2 节的核心原则亲手否定了 native 定位

**Spec 原文（第 2 节，核心设计原则）：**

> "LLM 只做语义判断与建议生成，所有执行动作走确定性 Skill——'可信 AI'边界，是 demo 要刻意展示的差异化。"

**Spec 第 2 节 Skill 拆分表也印证了这一点：**

| Skill | 实现方式 |
|---|---|
| `naming_validator` | 规则+正则，确定性 |
| `unit_normalizer` | 映射表，确定性 |
| `quality_rule_engine` | 规则引擎（类 DQM） |
| `merge_executor` | 确定性事务 |

六个 Skill 中四个是纯确定性逻辑，`attribute_validator` 也是规则，只有 `duplicate_matcher` 用了"向量语义相似度"作为规则匹配之外的第二通道。Agent 层的职责是"编排 Skill；LLM 做语义判断与建议生成"。

把 LLM 抽掉，系统的治理骨架——标准校验、质量检核、单位换算、归并执行——全部照常运行，只是少了"建议值"和"语义查重"两个增强项。**这不是 AI-native，这是传统规则型 MDM 加了一个 LLM 顾问层。**

真正的 AI-native 意味着核心治理逻辑由模型驱动、规则退居辅助——但那在主数据领域不可接受（见不合理之二）。于是 Spec 陷入悖论：越强调"可信 AI 边界"（LLM 不碰执行），越证明系统不是 AI-native；越追求 AI-native，越牺牲主数据赖以生存的确定性。Spec 选择了正确的工程方向（确定性优先），却给它贴了一个与实际架构矛盾的标签。对内立项尚可理解，对外售前 POC 时，懂行的客户一句话就能戳穿："你这不就是规则引擎加了个 LLM 接口吗？"

---

## 不合理之二：Spec 第 1 节的"三级自主度"在主数据零容错场景是事故隐患——高置信自动执行建立在未验证的校准假设上

**Spec 原文（第 1 节，增量线步骤 2）：**

> "标准 Agent 自动校验，高置信自动修正（自主度③）、中置信建议待批（自主度②）、合规放行"

**Spec 原文（第 1 节，步骤 5）：**

> "Copilot 审批：统一待办，建议带证据链+置信度，人批准/驳回/改判；三级自主度现场切换演示"

**Spec 第 3 节缺陷梯度表进一步把"高置信自动执行"落到了具体演示点：**

- 命名不规范：1 条高置信（自动修正）
- 计量单位错误：2 条高置信自动换算
- 疑似重复：1 条高置信（建议直接归并）

这个分流模型在演示中很漂亮，但它建立在一个 Spec 从未论证的假设上：**LLM 的置信度分数是校准良好的（well-calibrated）。** 实际上 LLM 的置信度（logprobs 或自评）以校准差著称——完全错误时同样可以给出"95% 置信"。在主数据场景中：

- **高置信自动换算单位**（Spec 第 3 节）：换算系数错误（如"个"↔"包"）会直接传导至采购、库存、BOM，造成生产停线或财务差异；
- **高置信建议直接归并**（Spec 第 3 节）：把两个看似相似但实际不同的物料合并会污染金标库，且旧码已通过 Spec 第 1 节步骤 6 的"BTP 分发"同步到消费系统，Key Mapping 已建立——撤销是分布式事务级别的难题；
- **自动修正命名/属性**（Spec 第 3 节）：紧固件强度等级 8.8 被"修正"为 10.9 直接关系产品安全和合规责任。

Spec 决策 #9 设定演示数据为"存量 10,000 条 / 增量 10 条/天"，缺陷预埋使得错误率为 0%。但决策 #4 的企业画像是"存量百万级"，真实场景中哪怕 0.1% 的高置信错误自动执行，每天也会产生数条需跨系统回滚的脏数据。**"高置信自动执行"在主数据领域不是效率优势，是事故隐患。** 而如果把所有写操作都改为人工确认，Spec 第 1 节引以为卖点的"三级自主度现场切换"（尤其是自主度③）就失去了演示意义。

---

## 不合理之三：Spec 第 3 节的缺陷设计是"波将金村"——"Agent 全能发现且自然分流"证明的是剧本质量，不是技术能力

**Spec 原文（第 3 节，核心原则）：**

> "缺陷'恰到好处'——Agent 全能发现，且按置信度自然分流到自动执行/待批/转人工三档。"

这是一个自我实现的预言：缺陷是按 Agent 的能力边界反向预埋的。Spec 第 3 节把重复簇限定为"8-10 组，三种典型形态"（同名异码、单位变体、描述近似），残缺记录限定为"约 2-3%"，领域限定为"紧固件+轴承两类，分类树 3 级"。在这样的封闭数据集上，任何系统——包括纯规则引擎——都能表现完美。

真实制造业数据治理的难点恰恰在 Spec 刻意排除的方向上：

1. **长尾模糊性**：Spec 第 3 节预设的重复对是"六角螺栓 镀锌" vs "六角头螺栓 电镀锌"——但真实数据中还存在"GB/T 5783 M8×30 锌"、"外六角螺丝 8×30 白锌"、"HEX BOLT M8X30 ZP"等数十种变体，其中有些标准不同、强度不同，语义相似度高但**不应合并**。LLM 在这些边界 case 上的误判率远高于演示，而误判的后果是错误归并（见不合理之二）；
2. **跨系统语义冲突**：同一物料在 ERP 用"件"、MES 用"个"、SRM 用"PCS"——Spec 第 2 节的 `unit_normalizer` 用映射表解决，但映射表本身的建立是业务协商问题，不是技术问题。Agent 无法单方面裁决两个工厂对同一单位的不同理解；
3. **缺陷不可枚举**：Spec 第 1 节只埋了四类缺陷（命名/单位/属性/重复），但真实历史遗留数据中有编码段错位、分类挂载错误、属性串栏、简繁体混用、全半角混用、OCR 录入错误等——规则和 LLM 都会遇到从未见过的模式，"全能发现"不成立。

Spec 决策 #4 写的是"存量百万级"，决策 #9 收窄为 10,000 条——这个收窄本身合理，但 Spec 没有说明 demo 的成功标准如何映射回百万级场景。一个在封闭数据集上 100% 发现率的 demo，对内立项会放大预期，对外 POC 时客户一旦接入自己的真实数据样本，演示效果会断崖式下跌。

---

## 不合理之四：Spec 决策 #8 的"自研轻量编排"低估了 Agent 系统的非线性失败——第 4 节说的"直线流程"只在 happy path 成立

**Spec 原文（决策 #8）：**

> "自研轻量编排，不引 LangGraph 等重框架"

**Spec 原文（第 4 节，后端新增）：**

> "自研轻量编排，不引 LangGraph 等重框架（编排是'感知→调 Skill→LLM 判断→路由'直线流程）"

"直线流程"只描述了 happy path。但 Spec 自己在第 1 节设计了**双线并行叙事**（增量线 + 存量线后台并行），在第 4 节给 Orchestrator 分配了"容错、trace"职责——这些需求已经超出了直线流程的范畴。Agent 系统的工程量 90% 在 failure path：

- **部分失败与状态恢复**：Spec 第 1 节步骤 2-4 中，归并 Agent 调了多个 Skill，前 2 个成功、第 3 个 LLM 调用超时——工单处于什么状态？已执行 Skill 的副作用如何回滚？Spec 第 4 节只说"容错"，但自研编排需要自己实现 saga/补偿事务；
- **幂等性**：Orchestrator 重试导致重复生成质量工单；`merge_executor`（Spec 第 2 节，确定性事务）网络超时触发重试，但第一次已写入金标库——重复归并破坏 Key Mapping；
- **并发冲突**：Spec 第 1 节的存量线"后台批量扫描"与增量线并行，可能同时操作同一金标记录（存量清洗发现某物料是重复簇，增量线正在为它创建申请），两个 Agent 对同一记录的写入需要乐观锁或分布式锁——Spec 第 4 节"不动现有表结构语义，只加新表新包"的约束让这个问题更难处理；
- **上下文与成本失控**：Spec 第 4 节的 `llm_gateway.py` 只提到"记录 token 成本与全链路 trace"，但 Agent 多轮调用 context 累积、token 成本不可预测时，需要限流、熔断、降级到 mock——这些是生产级网关的核心复杂度，不是"记录"一句话能覆盖的；
- **可观测性**：Spec 第 4 节新增 `agent_trace` 表，但跨 Agent 的 trace 传播、异步存量任务的 trace 关联、trace 本身在 10,000 条（未来百万级）规模下的存储和查询性能，都是工程问题。

LangGraph 等框架的"重"不是因为设计者笨，而是因为这些失败模式在真实系统中必然出现，框架内置了状态机、检查点、重试、人机协作断点（human-in-the-loop checkpoint）。Spec 第 1 节步骤 5 的"人批准/驳回/改判"本质上就是人机协作断点——这正是 LangGraph 的核心能力之一。自研轻量编排最终会在迭代中重新发明这些机制的半成品，且因无框架约束，三个 Agent 的错误处理风格会各不相同，技术债在 demo 转产品时集中爆发。

---

## 不合理之五：Spec 决策 #7 排除组织/流程/Owner，却在背景中声称对标 SAP MDG 做"主数据治理"——系统能力只是冰山一角

**Spec 原文（决策 #7）：**

> "只覆盖系统能力；系统之外的组织、流程、机制、Owner 等治理体系内容一律不做"

**Spec 原文（第 3 节注）：**

> "SAP MDG 产品只覆盖 4-7，1-3、8 靠平台扩展或实施补齐——'买了 MDG ≠ 治理成功'。"

Spec 敏锐地指出了"买了 MDG ≠ 治理成功"，却在决策 #7 中主动重蹈覆辙——甚至更严重，因为 AI 让"系统能自动做更多事"，反而制造了更多权责真空：

- **归并的权责问题**：Spec 第 1 节步骤 4 归并 Agent "给归并建议（主记录+合并字段+旧码映射保留）"，步骤 5 人批准——但谁有权批准？如果两个工厂属于不同事业部，合并后旧码失效影响工厂 B 的生产，属性模板却用工厂 A 的标准，这是业务决策权问题，不是置信度问题。Spec 决策 #7 把这个问题排除在外，demo 里"批准人"是一个没有组织上下文的抽象角色；
- **标准的协商问题**：Spec 第 1 节步骤 2 标准 Agent 校验"命名不规范"，但"标准"本身可能过时、有争议、各工厂不一致。系统可以校验"是否符合已发布标准"，但 Spec 全景图第 2 项"标准管理"只做了"定义、发布、版本"的系统侧，标准制定背后的跨部门协商过程没有 Owner 和流程，标准就是一纸空文；
- **整改的执行问题**：Spec 第 1 节步骤 3 质量 Agent"生成整改工单"——工单派给谁？Spec 第 4 节新增了 `quality_ticket` 表，但没有责任人模型、没有 SLA、没有升级机制。demo 里"人批准/驳回"是一个按钮，真实企业里这个按钮背后是岗位职责、绩效、跨部门博弈；
- **AI 建议的问责问题**：Spec 第 1节步骤 5 的"人批准/驳回/改判"看似有人在环，但如果 Agent 建议自动修正（自主度③）后发现错了，责任算谁的？算批准人的？算模型的？算 IT 部门的？Spec 第 4 节的 `agent_trace` 留痕解决了"可追溯"但没解决"谁负责"。没有治理机制，"AI 建议+人工确认"会变成"AI 建议、人无脑点确认、出了事 AI 背锅但 AI 不能背锅"的困局。

Spec 的双目标是"对内立项论证 + 对外售前 POC"。对内可以只论证技术可行性，但对外售前时，经历过治理失败的大型制造企业（正是 Spec 决策 #4 画像的客户）会直接问："这个归并建议出来了，我们厂里谁拍板？拍板错了谁负责？"——demo 答不上来，而这恰恰是项目成败的关键。

---

## 小结

| # | 对应 Spec 位置 | 不合理之处 | 核心矛盾 |
|---|---|---|---|
| 1 | 第 2 节核心原则 + Skill 表 | "AI-native"名不副实 | 确定性规则做骨架，LLM 只是外挂，却贴 native 标签 |
| 2 | 第 1 节步骤 2/5 + 第 3 节缺陷表 | 置信度路由在零容错场景危险 | LLM 置信度校准差，高置信错误自动执行=事故 |
| 3 | 第 3 节核心原则 + 数据设计 | Demo 缺陷预埋是波将金村 | 按能力反向设计数据，证明剧本而非技术 |
| 4 | 决策 #8 + 第 4 节编排描述 | 自研编排低估非线性失败 | 双线并行+容错+人机断点已非直线流程，failure path 是 90% 工程量 |
| 5 | 决策 #7 + 第 3 节注 | 排除组织维度却声称做治理 | 治理的本质是权责，"买了 MDG≠成功"的教训自己没吸取 |

**一句话总结**：Spec 在工程上做了正确的选择（确定性优先、轻量编排、聚焦系统能力），但在叙事上给这些选择套了一层与实际不符的"AI-native"包装。Demo 应该展示的不是"AI 能自动做多少"，而是"AI 如何让治理责任人更快、更准、更有依据地做决定"——前者是演示效果，后者才是产品价值。
