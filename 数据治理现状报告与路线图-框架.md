# 《数据治理现状报告 + Collibra 验证与落地路线图》框架与要点说明

> 本文档为立项与现状评估用的结构说明；请按贵司实际替换方括号及示例内容。

---

## 二、数据治理现状报告（正文结构）

### 2.1 组织与职责

- 数据治理牵头部门、业务域 Owner（财务/人力/供应链）、IT 与 DBA 接口人。
- 当前是否有数据标准、质量工单、变更流程（有/无/部分），用简短表格列出。

| 项 | 现状（有 / 无 / 部分） | 简要说明 |
|----|------------------------|----------|
| 数据标准 | | 字段定义、编码规则、权威源等是否成文、是否可执行 |
| 数据质量工单 | | 见下方 **「数据质量工单」填写说明与术语备注** |
| 变更流程 | | 改标准、改模型、改接口时的评审、发布、审批机制 |

#### 数据质量工单 — 填写说明与术语备注

**定义：** 数据质量工单指：发现或怀疑存在数据质量问题时，通过**固定流程**进行记录、分派、跟踪直至关闭的工作单（对象是**数据**，类似 IT 故障单/需求单）。

**单条工单通常建议包含：**

| 要素 | 说明 |
|------|------|
| 提出人 | 业务 / 报表用户 / 数据团队等 |
| 问题描述 | 如：主数据重复、客商税号不一致、必填为空、跨系统不一致等 |
| 影响范围 | 涉及库表/系统、是否影响报表或合规 |
| 优先级与期限 | 紧急程度、期望完成时间 |
| 处理责任人 | 源系统 Owner、主数据管理员等 |
| 状态 | 如：新建 → 处理中 → 待验证 → 已关闭 |
| 根因与措施（可选） | 根因分析、整改措施、是否需修订数据标准或改造系统 |
| 证据与 SLA（可选） | 规则运行结果/样例（注意脱敏）、截图；响应/解决时限及超期升级规则 |

**常见载体（用于判断填「有」或「无」）：**

- Jira、ServiceNow、飞书、企业微信、OA 等**专用流程或表单**；
- 数据质量/治理平台自带的**问题闭环、任务**（若与 Collibra 等工具集成也算）。

**与相邻概念区分（便于填表）：**

- **数据标准**：字段怎么定义、怎么编码、谁为权威 —— **规矩本身**。
- **数据质量工单**：不合规矩或出现异常时，**如何接单、修复、验收** —— **运营闭环**。
- **变更流程**：改标准、改模型、改接口时，**如何评审、发布、谁批准** —— **变更治理**。

若仅有邮件/口头沟通、无统一编号与状态跟踪，建议填 **「无」** 或 **「部分（非正式）」**。

---

### 2.2 系统与数据资产清单（只读视角）

| 系统 | 主要模块 | 关键实体（示例） | 连接方式（JDBC/API/只读副本） | 元数据可获取性 |
|------|-----------|------------------|-------------------------------|----------------|
| ERP | （填写） | 客户、供应商、会计科目… | （填写） | （见下方说明） |
| HR | （填写） | 员工、部门、岗位… | （填写） | （见下方说明） |
| SRM | （填写） | 物料、订单、入库… | （填写） | （见下方说明） |

#### 「元数据可获取性」是什么意思？该怎么填？

这里说的 **元数据**，指**描述数据结构的信息**，不是业务明细行。例如：库名/表名/视图名、列名、数据类型、主键外键、索引、注释（comment）、接口字段说明、枚举值说明等。

**「可获取性」**问的是：对 Collibra（或数据团队）来说，这些元数据**好不好拿到、好不好用**，而不仅是「数据库能不能连上」。可从下面几方面自检，用**一句话或「高 / 中 / 低」**写进表格即可。

| 考量维度 | 你可以这样回答（示例） |
|----------|-------------------------|
| **技术能否读到** | 能否用只读账号查系统目录（如 `information_schema`）、数据字典表或元数据 API；是否只能连库但看不到注释 |
| **完整程度** | 表结构有，但缺业务注释；或大量是自定义视图/接口，文档与实现是否一致 |
| **出口与权限** | 是否允许采集元数据出域；是否需单独审批；是否仅限 VPN/堡垒机 |
| **非标/打包系统** | SAP 等是否需通过专用层（如 CDS、BW、中间件）才能暴露可读结构 |
| **文档替代** | 若没有自动字典，是否有较新的 ER 图、接口文档可人工录入或对照 |

**填写示例（可直接改写）：**

- **高：** 只读账号可访问数据字典；表/列注释较全；或接口 Swagger/OpenAPI 完整，Collibra 可对接采集。  
- **中：** 能读到表结构，但注释大量为空；部分模块只有 PDF/旧版 Excel 字典，需业务确认。  
- **低：** 仅能连测试库或部分 schema；核心结构在厂商黑盒或需二次开发才能暴露；几乎无文档。

**你不需要「回答我」**——这是写进《现状报告》里给**读者（管理层、厂商、实施方）**看的自检结论。若你愿意在对话里练一练，可以按「ERP / HR / SRM」各发一句现状（例如：能连 Oracle 只读，注释约 30% 有），我可以帮你压成表格里可用的正式表述。

#### 立项材料表述（Collibra 元数据与目录建设，可直接引用）

以下表述面向**立项申请、可行性研究、PoC/建设方案、项目章程**正文，可按篇幅整段引用或拆章使用；方括号内请替换为贵司名称、系统名与政策要求。

---

**【管理层摘要 · 一句话】**  
元数据获取方式：**连接器自动采集** + **平台内资产治理与业务语义补全** + **规则/AI 建议与业务及 IT 人机协同确认（关系与血缘）**，在符合只读、授权范围与安全合规前提下，形成**可检索、可解释、可审计**的企业数据目录与关键链路视图。

---

**【立项正文 · 详细说明】**

**1）目标与边界**  
本项目（或 PoC）在 Collibra 数据智能平台上建设企业级**数据目录（Data Catalog）**及相关治理能力。元数据工作的直接目标包括：统一呈现 ERP、HR、SRM 等授权系统中的**技术结构**与**业务含义**；支撑跨系统的**主数据一致性分析**与**数据质量规则落地**的前置基础；为报表差异排查、影响分析、合规审计提供**可追溯的血缘与责任体系**。  
边界说明：技术元数据以**企业明确授权的数据源与对象范围**为准；业务明细数据遵循**最小必要、抽样或脱敏**等安全策略（与信息安全及法务要求对齐）；**不**将未经授权的敏感数据上传至公网或大模型服务（若启用 AI 能力，须在**企业批准的网络与部署形态**下实施，并在方案中单独说明）。

**2）自动采集：连接器与可采集内容**  
通过 Collibra **连接器（Connector）**对接经审批的数据源（典型为**数据库只读访问**、在支持场景下包含 **API/元数据接口**、BI/ETL 等，**具体以厂商支持矩阵与贵司实际采购模块为准**），对授权范围内的对象执行**增量或定期全量**的元数据采集，形成目录的**自动化骨架**，减少手工抄录与版本漂移。  
自动采集侧重**技术元数据**，通常包括但不限于：**库 / Schema / 表 / 视图**等对象清单；**列级**名称、数据类型、长度精度、可空、默认值（以数据源能力为准）；数据库可暴露的**主键、外键、索引**等约束信息；库表或字段上的**注释（Comment）**（若建设方在源库维护）；部分连接器还可采集与对象相关的**技术属性**（如分区、统计信息等，视连接器与数据库类型而定）。  
说明：**自动采集的是「结构描述」为主**，不等同于自动获得完整业务定义；若源系统注释缺失或严重滞后，必须在后续「平台内治理」中补全，并在现状报告中如实评估**元数据可获取性（高/中/低）**及对工期的影响。

**3）平台内治理：从「技术对象」到「业务资产」**  
在完成自动采集后，在 Collibra 内将技术对象治理为业务可理解的**数据资产**，主要包括但不限于：  
- **业务化展示**：为表、字段等补充或校正**业务名称**、**业务定义/说明**、**计算口径说明**（尤其针对指标类资产若后续扩展），避免仅展示技术命名。  
- **责任体系**：为关键资产指定或关联**数据 Owner、Steward（数据管家）**、**技术联系人**，并与组织职责（RACI）对齐，确保目录上的对象「有人认、有人问、有人改」。  
- **安全与合规标签**：依据企业数据分类分级制度，标注**敏感级别**（如个人信息、重要数据、商业秘密等类别以贵司制度为准），支撑后续权限展示、脱敏策略与审计要求。  
- **业务术语挂接**：将**业务术语表（Glossary）**中的概念与**物理字段/表**建立映射（同义、包含、计算派生等关系），实现「说业务话能查到数、点技术字段能看懂业务含义」。  
- **补充材料入库**：对连接器无法覆盖或质量不足的部分，通过**受控的批量导入**（如 Excel 模板）、接口或规范化的设计文档摘录，将经评审的内容纳入目录，与自动采集结果**统一版本管理**，避免「文档在网盘、真相在库里」两张皮。

**4）数据关系与血缘：规则解析、AI 辅助与人工确认**  
**跨系统数据关系**与**数据血缘**是立项与 PoC 的重点验证项，其实施逻辑建议写清为「**机器先行、人工兜底、持续运营**」，避免管理层误解为「全自动零校验」或「全靠人工画图」。  

- **关系（逻辑/物理关联）**：在具备主外键、命名规范、接口主键对照等线索时，可通过元数据解析与（若采购）**AI/语义匹配**生成**候选关系**（例如 ERP 员工号与 HR 工号、物料号在多系统中的对应字段）；业务与 IT 对候选结果进行**确认、合并、否决或标注置信度**，形成企业认可的**跨系统关系视图**（可用于主数据对账与影响分析的基础）。  
- **血缘（端到端链路）**：在可获取 **SQL、存储过程、ETL 作业、报表定义、部分 API 编排**等工件的前提下，通过解析与平台能力生成**字段级或表级**血缘；无法自动解析的黑盒环节（如部分封装逻辑、第三方 SaaS），通过**人工登记血缘片段**或**附件说明**补齐，并在目录中明确**可信度与更新责任**。  
- **人机协同机制**：建议约定**抽检比例或清单制验收**（例如关键报表链路 N 条端到端核对）、**异议升级路径**（业务口径争议由数据治理组织裁决）、**变更联动**（源系统表结构变更后触发目录同步与血缘重算/复核）。  

**5）安全、合规与运维**  
- **账号与权限**：数据源侧采用**只读**技术账号，权限**最小化**至 PoC/建设所需 Schema 或对象列表；平台侧按角色控制编目、审批与查看范围。  
- **网络与部署**：Collibra **私有化平台**部署于**企业内网环境**（或经批准的专用网络分区），与**现有 IT 基础设施**实现**内网互通**；与 ERP、HR、SRM 等源系统的连接器访问、服务调用均在**企业可控网络边界**内完成。防火墙策略、端口白名单、堡垒机跳转、DNS 与证书等由 IT 在《网络与安全专项方案》中单列并负责运维衔接。  
- **审计与留痕**：保留采集任务记录、关键人工确认记录（谁于何时确认关系/血缘）、敏感标签变更记录，满足内审与外审抽样要求。  
- **同步策略**：明确元数据**刷新频率**（如每日/每周）与**重大变更后的即时同步**流程，避免目录长期陈旧导致治理失效。

**6）组织分工（建议写入项目职责表）**  

| 角色 | 与元数据工作的关系 |
|------|---------------------|
| 企业 IT / DBA | 开通只读连接、对象范围审批、性能与安全配合、结构变更通告 |
| 数据治理 / 数据管理 | 目录标准、术语与分级规范、抽检与验收、跨部门协调 |
| 业务域 Owner / Steward | 业务名称与定义、关系与血缘确认、口径争议裁决参与 |
| Collibra 实施方 / 架构师 | 连接器配置、采集范围设计、血缘/关系方案落地与培训移交 |

**7）阶段交付物（便于立项写「验收/里程碑」）**  
- 《授权数据源与采集范围清单》（含不含项说明）  
- 《自动采集覆盖报告》（对象数、字段数、注释覆盖率等基线指标）  
- 《数据目录与术语挂接进度》（分系统、分域）  
- 《关键关系与血缘确认清单》（含人工确认签字或系统记录导出）  
- 《敏感数据识别与分级标签策略执行说明》（与贵司制度对齐）  

**8）PoC 与正式建设的差异（可选段落）**  
PoC 阶段优先验证**连接器可行性、元数据可获取性、关键链路血缘与关系的人机协同闭环**，范围控制在约定系统与抽样对象；正式建设阶段扩展**资产覆盖范围、术语与分级体系全覆盖、与数据质量工单及变更流程的平台化衔接**，并将刷新与运营 KPI 纳入常态化考核。

#### 部署架构表述（立项 / 可研可直接引用）

- **简版（第一人称，可直接用于内部立项申请）：**  
  Collibra 私有化平台部署于**我司内网环境**，与**现有 IT 基础设施**通过**内网**连接。

- **详版（对外或可研正文，可与安全章节衔接）：**  
  Collibra 采用**私有化部署**，平台运行于**企业内网**（或经安全评审批准的**专用网络分区 / 私有云 VPC**），与现有 **IT 基础设施**实现**内网互联**；与业务源系统之间的元数据采集、任务调度及必要服务通信**不出企业可控网络边界**（若存在跨分区流量，须在方案中明确路径与安全控制措施）。具体拓扑、防火墙与堡垒机策略、高可用与灾备要求见《网络与安全专项方案》。

---

## 三、技术路线备选：开源私有化（OpenMetadata）要点纪要

> 本节与 **Collibra 商业路线**可并行用于立项比选或分阶段建设。前提假设：**数据源以 SQL Server、MySQL 为主**；**当前无数仓 / ETL**；要求**私有化部署**；工作流需**中文体验**时优先借助现有协同平台。

### 1. 平台选型结论

在以上前提下，**首先考虑 OpenMetadata** 作为开源目录与基础治理平台；是否最终采用须以**小规模 PoC**（双库、限定 schema、验证中文界面与连接器）结论为准。若更看重元数据模型扩展与特定生态集成，可并列评估 **DataHub**。

### 2. 为何以「连库 / JDBC 类采集」为主，而非业务系统 API？

- 关系型数据库的**结构元数据**（库表字段、类型、主外键、注释等）的**权威来源**是数据库**系统目录**；通过 **JDBC（及驱动）**读取目录，是**最标准、最完整**的路径。  
- ERP / CRM / SRM / MES / WMS 的 **REST/SOAP 等业务 API** 通常面向**业务单据与查询结果**，**不等价**于「全量表模型 + 字段级技术字典」；即便存在元数据类接口，也多为**碎片化**，需**按系统单独对接**。  
- 结论：**针对 SQL Server / MySQL，默认采用连库元数据采集**；仅在**无库可读、仅有 API** 时，再考虑 **API 摄取或自定义 Connector**（实施成本通常更高）。

### 3. OpenMetadata 能力与边界（术语 / Owner / 标签 / 质量）

平台一般提供 **术语表（Glossary）**、资产 **Owner / 域**、**标签与分类**、**基础数据质量**等能力；具体以**所部署版本**为准。与 Collibra 等商业一体化方案相比，宜表述为：**能力具备，深度与运营化程度需 PoC 逐项核对**，并列出「必用功能清单」作为验收条目。

### 4. 血缘：无数仓 / ETL 时的现实做法

- **端到端报表血缘**、**跨系统字段级血缘**在缺乏 SQL/ETL/调度解析信号时，**自动能力有限**。  
- **人工在目录中维护血缘**（节点与边、或等价配置）：**操作本身不难**，难点在于**资产规模大、变更频繁时的持续维护**与**责任制度**（谁建、谁改、谁验收）。  
- **务实策略**：先选定 **少数关键链路** 手工建全，用于管理与审计演示；后续若建设数仓或 ETL，再逐步接入自动血缘信号。

### 5. 跨系统关系：规则 + 人工确认

在**库名不同、无统一企业模型**时，跨 ERP/CRM/SRM/MES/WMS 对应关系宜采用：**匹配规则**（同名键、主外键、映射表、可控模糊规则等）生成**候选关系**，由业务与 **IT 人工确认、合并或否决**，形成可信任的跨系统关系视图。该策略与是否开源无关，属于**治理方法**。

### 6. 中文工作流与「数据资产真相源」分工（推荐表述）

**元数据与目录**由开源私有化平台承担，作为企业**数据资产（技术侧）的真相源之一**；**中文审批、会签、超时、抄送**等强流程需求，建议走**现有 OA / 钉钉 / 企业微信**等协同平台，通过 **Webhook / API / 工单编号回写** 与目录联动。  
**立项表述可引用：** *元数据与目录用开源私有化；中文工作流走现有 OA/钉钉/飞书，目录负责数据资产真相源，避免指望单一开源产品完整替代 Collibra 类商业产品的流程体验。*

### 7. 连接器范围与扩展；术语 / Owner 是否可加强

- **连接器**：除 SQL Server、MySQL 外，OpenMetadata 通常还支持多种数据库、消息、BI 及部分 SaaS；**以官方文档 Connectors 列表及当前版本为准**。企业侧以 **SQL Server + MySQL** 为起点，风险可控。  
- **术语与 Owner**：一般支持多术语、多层级词汇表及多角色；若默认模型不足，可通过**自定义属性**、**域划分**或与**外部主数据 / 工单系统**对照扩展。  
- **新增接入方式**：升级版本、启用相应连接器；或通过**自定义摄取 / SDK / 调用平台 API** 接入特殊数据源。

### 8. 业务指标治理（Metrics）：OpenMetadata 能做什么（立项表述）

企业「**核心指标 / 业务指标治理**」在管理层常归结为三类诉求：**口径统一**、**找得到负责人**、**能追到源表（及下游消费）**。OpenMetadata 通过 **Govern → Metrics（指标实体）** 支撑其中的**元数据与责任体系**部分；与 **Data Insights 中的 KPI**（如描述覆盖率、Owner 覆盖率等「目录健康度」）**不是同一概念**，立项与培训时需分开说明。

| 管理诉求 | OpenMetadata 中的对应能力（摘要） | 说明与边界 |
|----------|-------------------------------------|------------|
| **口径统一** | 指标的**名称、展示名、业务说明**；**计算公式 / 表达式**；可选 **SQL**；**粒度**（如按日/周/月）；**指标类型**与**计量单位**；与 **术语表（Glossary）** 及**其它数据资产**的关联 | 侧重**「指标作为治理对象」的登记与版本留痕**；**实际取数、多维下钻、统一计算服务**仍依赖 **BI、数据库或专用指标中台**，宜在架构中写明分工。 |
| **找得到负责人** | 为指标指定 **Owner（团队/个人）**；可结合**域、标签**做检索与职责边界 | 满足**认责与对口**；审批流若需**中文、会签、超时**，仍建议与 **OA / 钉钉 / 飞书** 联动（见本节第 6 点）。 |
| **能追到源表** | **血缘**：文档能力支持将指标与**表、管道（若后续建设）、看板**等关联，形成「**源数据 → 指标 → 消费端**」的可视追溯 | **当前无数仓 / ETL** 时，管道类节点可能较少，宜以 **库表 ↔ 指标 ↔（若有）报表/看板** 为主，**优先保障关键指标**的链路完整；全量自动化血缘需后续接入 ETL/调度或人工维护策略。 |

**立项/可研可引用段落：**  
在采用 OpenMetadata 作为私有化数据目录的前提下，**业务指标治理**以平台 **Metrics** 能力为主：统一登记**指标定义、计算口径（含公式与可选 SQL）、粒度与单位**，明确 **Owner**，并与**术语及底层表资产**关联，支持**版本管理与血缘展示**，作为企业**指标元数据的权威登记处**之一。指标**在线计算与统一服务化**由既有 **BI / 数据库 / 指标引擎**承担时，须在方案中界定「**定义与认责在目录、运行与消费在分析层**」，避免期望单一目录平台替代完整指标中台。

---

## 四、管理层关切：数据加载、问题识别、自动清洗、效果校验

> 本节将决策层常问的四个环节写成**可立项引用**的表述，并区分：**目录/治理平台**能覆盖的范围 vs 必须依赖 **ETL、主数据（MDM）、源系统改数** 的部分。当前企业场景：**SQL Server / MySQL**、**无数仓 / ETL** 时，部分能力需**分阶段建设**或**外置工具**补齐。

### 1. 四个环节的关系（闭环）

| 环节 | 一句话目的 | 常见误区 |
|------|------------|----------|
| **数据加载** | 把「该进平台的数据」稳定、可审计地接入 | 把「加载」只理解成**业务大表全量入仓**；忽略**元数据/抽样剖析**也是一种加载 |
| **问题识别** | 持续发现不符合规则或统计异常的数据 | 以为**一次盘点**即永久有效；忽略**规则版本与源系统变更** |
| **自动清洗** | 减少人工逐行修改，提高修复效率与一致性 | 以为平台会**不经审批直接改生产库**；实际生产环境多需**工单 + 源系统 Owner** |
| **效果校验** | 证明清洗/整改**真的生效**且**不引入新风险** | 只凭主观确认；缺少**规则重跑、对比指标、抽检签字** |

推荐在方案中画闭环：**加载 → 识别 → 清洗（或分派整改）→ 校验 →（规则/模型更新）→ 再识别**，并写明每步的**责任角色**与**系统载体**。

### 2. 数据加载：建议拆成两层写清

**（1）元数据与剖析数据的加载（目录 / 治理平台主责）**  
- 通过**连接器**定期将 **SQL Server / MySQL** 的**表结构、注释、主外键**等写入目录；按需对授权对象做**画像（Profiling）**或**抽样行剖析**（须符合**只读、最小必要、脱敏**策略）。  
- **结论：** 在**无数仓**阶段，仍可实现「**结构进目录、关键表可体检**」，不等同于建设数据仓库。

**（2）业务明细的批量加载（通常不是目录的核心职责）**  
- 若指**全量业务数据入仓、宽表整合**，属于**数仓 / ETL / 集成平台**建设范围；**当前无 ETL** 时，应在立项中写为**后续阶段**或与**专用集成项目**捆绑。  
- **过渡做法：** 保留**源系统为权威**；目录侧以**元数据 + 质量规则在源库或只读副本上执行**（视性能与安全评审而定）。

**给老板的一句话：**  
「加载」我们分**两步走**：先把**元数据和关键剖析**稳定进治理平台，让资产可见、可管；**全量业务数据入仓**单独立项，不混在目录一期里讲。

### 3. 问题识别：规则、画像与工单联动

- **能力来源：** 治理平台（如 OpenMetadata 的数据质量、剖析；或 Collibra 等商业 **DQ** 模块）可配置**规则**（完整性、唯一性、格式、参照一致性等）并结合**统计分析**发现异常；亦可由 **SQL 脚本 / 定时任务** 在只读环境执行，结果**回写或关联**到目录中的资产。  
- **跨系统问题**（ERP/CRM/SRM 等）往往依赖**映射规则 + 对账**，识别结果应进入**数据质量工单**（见本文 **§2.1 数据质量工单**），避免只在邮件里流转。  
- **无数仓时**：识别仍可做，但**复杂跨表整合类**规则可能受限，宜优先**主数据域、高业务影响表**。

### 4. 「自动清洗」：预期应对齐为「自动化辅助 + 可控变更」

**事实边界（须在立项中写清，避免验收扯皮）：**  
- **数据目录 / 元数据平台**的首要职责是**发现、记录、分派、追踪**，**通常不承担**对生产库的**静默自动改数**。  
- 生产上常见的「清洗」落地形态包括：  
  - **源系统内修复**（由业务/IT 按工单在 ERP/CRM 等界面或接口修正）——**最符合权责**；  
  - **主数据（MDM）或专用清洗作业**在**受控环境**执行，经审批后**同步回写**——需 **MDM/ETL** 能力；  
  - **标准化规则**（如编码映射、去重合并）在**中间层或 staging** 执行，再**发布**——仍属集成/数仓范畴。  

**可对外表述为：**  
本项目中的「**自动清洗**」指：**自动识别问题、自动推荐规则或修复建议、自动触发审批与工作流**；**对生产数据的实际变更**须在**源系统或经批准的 MDM/ETL 通道**中完成，并保留**审计痕迹**。若老板坚持「机器直接改库」，须单独进行**安全与合规评审**，一般不推荐作为默认模式。

### 5. 效果校验：如何向老板证明「治好了」

建议至少包含四类证据（可写入验收标准）：

1. **规则重跑**：同一批质量规则在整改前后各执行一次，**问题条数/严重度**下降可量化。  
2. **关键指标对比**：如重复率、空值率、主数据一致率等**前后对比**（抽样或全表视性能而定）。  
3. **业务抽检**：业务 Owner 对**样本记录**签字或系统确认「可接受」。  
4. **回归与防复发**：源系统或映射规则是否同步修订，避免**只清表面、根因仍在**；必要时更新**规则版本**并纳入**变更流程**。

### 6. 与 Collibra / OpenMetadata 路线的对应（便于汇报二选一或组合）

| 关切点 | Collibra 路线（摘要） | OpenMetadata 路线（摘要） |
|--------|------------------------|----------------------------|
| 加载 | 连接器采元数据；DQ/剖析依采购模块；业务入仓仍靠集成项目 | 连接器采元数据 + 剖析/质量测试；无内置「企业级 ETL」 |
| 问题识别 | 商业 DQ、工作流与目录一体化程度通常较高（视合同） | 内置质量与剖析能力，复杂场景可能需自定义或外部调度 |
| 自动清洗 | 多通过**工作流 + 认责**落到源系统；直接改数仍需谨慎约定 | 同上；更需强调 **OA/工单 + 源系统修复** |
| 效果校验 | 报告、仪表板、审计与项目验收对齐 | 规则结果历史、测试套件重跑 + 管理报表（视版本） |

### 7. 立项可引用段落（整段）

数据治理闭环包含 **数据加载、问题识别、自动清洗与效果校验** 四步。**加载**宜区分 **元数据与剖析数据进入治理平台**（实现资产可见、可检）与 **全量业务入仓**（属数仓/ETL 建设，分阶段推进）。**问题识别**依托质量规则、画像与跨系统对账，结果须进入**工单闭环**。**自动清洗**在本方案中指 **自动发现问题、推荐规则并驱动审批与分派**；对生产数据的变更以 **源系统或经批准的 MDM/ETL 通道** 为准，确保可追溯、可审计。**效果校验**通过 **规则重跑、量化指标对比、业务抽检与根因修复** 四方证据验收，避免一次性盘点后即无人维护。

---
## 五、主数据域现状与评估口径（可直接填报）

### 5.1 评估对象与权威源（System of Record）

| 数据域 | 涉及系统 | 建议权威源 | 当前分发系统 | 典型问题 |
|--------|----------|------------|--------------|----------|
| 员工主数据 | HR、ERP、SRM | HR | ERP、SRM、报表系统 | 工号映射不一致、离职状态滞后 |
| 客户主数据 | ERP、CRM（若有） | ERP/CRM（按组织约定） | SRM、财务报表 | 同客户多编码、税号缺失 |
| 供应商主数据 | ERP、SRM | SRM/ERP（按采购流程约定） | ERP、付款系统 | 供应商合并不及时、黑名单同步延迟 |
| 物料/产品主数据 | ERP、SRM、MES、WMS | ERP/MES（按生产组织约定） | WMS、成本系统 | 物料编码重复、计量单位不一致 |
| 组织/部门/成本中心 | HR、ERP | HR（组织）+ ERP（成本中心） | 全系统 | 部门与成本中心映射缺失 |

### 5.2 域内治理成熟度自评（1-5 分）

| 维度 | 1 分（初始） | 3 分（可用） | 5 分（成熟） | 当前评分 |
|------|--------------|--------------|--------------|----------|
| 标准化 | 无统一定义 | 关键字段有标准 | 统一标准+版本机制 | |
| 一致性 | 高频冲突 | 可识别冲突并处理 | 自动识别+闭环治理 | |
| 责任归属 | 无 Owner | 关键域有 Owner | 全域 Owner+Steward 机制 | |
| 工单闭环 | 口头/邮件 | 平台化受理 | SLA+审计+复盘 | |
| 变更治理 | 无审批链 | 关键变更可评审 | 全量变更联动目录与规则 | |

---

## 六、元数据关系与数据血缘（现状 + 目标）

### 6.1 关系识别现状

- **现状判断**：主外键定义不完整、跨系统映射依赖人工经验。  
- **短期目标**：以同名键、映射表、命名规则生成候选关系，再由业务与 IT 联合确认。  
- **输出成果**：跨系统关系图（ERP↔HR↔SRM）+ 关系确认清单（含置信度）。

### 6.2 血缘能力现状

| 血缘类型 | 当前能力 | 短期做法 | 长期做法 |
|----------|----------|----------|----------|
| 表级血缘 | 可部分识别 | 先覆盖关键表 | 全域自动刷新 |
| 字段级血缘 | 自动能力有限 | 关键链路人工补链路 | 接入 ETL/调度信号后自动化 |
| 报表追溯 | 依赖人工排查 | 建立 N 条端到端样例 | 纳入常态化影响分析 |

### 6.3 验收建议

- 至少形成 **N 条关键链路**（建议 10-20 条）端到端可追溯；  
- 每条链路都应有**确认责任人**与**最后更新时间**；  
- 对无法自动解析环节，需标注“**人工维护**”与“**可信度等级**”。

---

## 七、数据质量（DQ）基线与规则体系

### 7.1 DQ 维度与典型规则

| DQ 维度 | 规则示例 | 说明 |
|---------|----------|------|
| 完整性 | 必填字段非空 | 员工号、供应商税号、物料编码不得为空 |
| 唯一性 | 主键/业务键去重 | 客户编码、供应商编码唯一 |
| 格式有效性 | 格式/字典匹配 | 电话、统一社会信用代码、日期格式 |
| 一致性 | 跨系统字段一致 | HR 工号与 ERP 员工编码映射一致 |
| 参照完整性 | 外键关联有效 | 订单中的物料编码必须存在于主数据 |
| 及时性 | 数据更新时效 | 关键主数据变更 T+1 同步达标 |

### 7.2 DQ 执行与闭环机制

1. 规则上线前：业务确认口径与阈值；  
2. 规则执行后：自动生成问题清单与严重级别；  
3. 进入工单：派单给源系统 Owner；  
4. 修复后复跑：验证是否达标；  
5. 复盘：更新规则或标准，防止复发。

### 7.3 建议首批规则数量（PoC）

- 主数据域：每域 5-10 条；  
- 事务数据结构与关联：每类 3-5 条；  
- 合计建议：**30-60 条可执行规则**，优先高风险字段。

---

## 八、安全合规（私有化、只读、审计）

### 8.1 合规控制矩阵

| 控制项 | 要求 | 落地方式 |
|--------|------|----------|
| 部署边界 | 私有化内网 | 平台部署在企业内网/专用分区 |
| 数据访问 | 最小权限只读 | DB 账号仅 SELECT，限定 schema/对象 |
| 数据处理 | 不全量搬运 | 以元数据+抽样剖析为主 |
| 数据外发 | 禁止未授权外传 | 禁公网上传，禁共享账号 |
| 操作留痕 | 可审计 | 采集日志、确认日志、变更日志 |
| 安全评审 | 必须前置 | 网络策略、端口、证书、堡垒机审批 |

### 8.2 必备制度联动

- 数据分级分类制度；  
- 数据质量工单制度；  
- 数据标准与变更评审制度；  
- 定期审计与月度治理例会机制。

---

## 九、差距分析与风险台账（可直接用于立项）

### 9.1 关键差距

- 目录与术语未统一；  
- 跨系统关系依赖人工经验；  
- 质量问题无统一工单闭环；  
- 变更与血缘更新未联动；  
- 缺少可对管理层汇报的量化指标。

### 9.2 风险台账模板

| 风险 | 影响级别 | 触发信号 | 应对策略 | 责任人 |
|------|----------|----------|----------|--------|
| 权限审批延迟 | 高 | 周期超过计划 | 拆分范围、优先核心域 | IT 负责人 |
| 业务参与不足 | 高 | 评审会缺席 | 锁定 Owner、周例会 | 数据治理负责人 |
| 自动识别准确率低 | 中 | 候选关系误判高 | 人工抽检+规则优化 | 实施架构师 |
| 合规疑虑 | 高 | 安全评审未通过 | 前置评审、分级脱敏 | 安全负责人 |
| 进度失控 | 中 | 里程碑延后 | 范围冻结+变更管理 | 项目经理 |

---

## 十、Collibra 验证设计（PoC 验收口径）

### 10.1 验证项与验收证据

| 验证项 | 验收标准（示例） | 证据 |
|--------|-------------------|------|
| 元数据自动采集 | 目标范围覆盖率 ≥ 85% | 采集报告 |
| 关系识别准确率 | 关键域准确率 ≥ 85% | 抽检记录+确认清单 |
| 血缘可追溯 | 关键链路 N 条可追溯 | 链路截图+确认记录 |
| 质量识别 | 发现并闭环 ≥ 20 项问题 | 工单台账+复跑结果 |
| 合规达标 | 只读、内网、留痕全满足 | 安全审计记录 |

### 10.2 演示场景建议

- 场景 A：跨系统字段关系自动识别 + 人工确认；  
- 场景 B：报表异常回溯到源表；  
- 场景 C：主数据重复/缺失自动识别并派单；  
- 场景 D：修复后规则复跑对比前后指标。

---

## 十一、路线图（3/6/12 个月）

### 11.1 分阶段目标

| 阶段 | 周期 | 目标 | 关键交付 |
|------|------|------|----------|
| 阶段 0（PoC） | 1 个月 | 验证可行性与边界 | 验收报告、路线图、预算框架 |
| 阶段 1（基础治理） | 3 个月 | 目录、术语、Owner、首批规则 | 目录基线、工单机制、周报体系 |
| 阶段 2（重点域深化） | 6 个月 | 主数据一致性、关键血缘 | 域级治理报告、SLA 管理 |
| 阶段 3（规模化） | 12 个月 | 扩系统、扩规则、运营化 | 治理运营 KPI、季度复盘 |

### 11.2 建议推进原则

- 先主数据、后事务扩展；  
- 先高风险域、后低风险域；  
- 先机制跑通、后规模扩张。

---

## 十二、KPI 指标体系（管理层看板）

### 12.1 KPI 建议清单

| 指标类别 | 指标名称 | 建议口径 |
|----------|----------|----------|
| 覆盖类 | 元数据覆盖率 | 已采集对象 / 目标对象 |
| 覆盖类 | Owner 覆盖率 | 已指定 Owner 资产 / 关键资产 |
| 质量类 | 问题发现率 | 周期内发现问题数 / 检测对象数 |
| 质量类 | 问题闭环率 | 已关闭工单 / 总工单 |
| 质量类 | 复发率 | 重复出现问题 / 已关闭问题 |
| 时效类 | SLA 达成率 | 按时关闭工单 / 总工单 |
| 价值类 | 人工工时下降 | 基线工时 - 当前工时 |
| 合规类 | 审计通过率 | 通过检查项 / 总检查项 |

### 12.2 报告节奏建议

- 周报：进度、问题、风险、待决策事项；  
- 月报：KPI 趋势、重点整改、成本与收益；  
- 季报：治理成熟度评估与路线图调整建议。

---

## 附录（模板）

### 附录 A：系统与表清单模板

| 系统 | 库/Schema | 表名 | 业务含义 | 是否纳入 PoC | 负责人 |
|------|-----------|------|----------|----------------|--------|

### 附录 B：关系确认清单模板

| 源系统字段 | 目标系统字段 | 关系类型 | 置信度 | 确认人 | 日期 |
|------------|---------------|----------|--------|--------|------|

### 附录 C：质量工单台账模板

| 工单号 | 问题描述 | 严重级别 | 责任人 | 状态 | 关闭时间 | 验证结论 |
|--------|----------|----------|--------|------|----------|----------|

### 附录 D：PoC 验收签字页（建议）

- 业务负责人签字：________________  日期：________  
- IT 负责人签字：__________________  日期：________  
- 数据治理负责人签字：____________  日期：________  
- 项目经理签字：__________________  日期：________
