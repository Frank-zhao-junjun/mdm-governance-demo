# 基于 Ralph Loop 的物料主数据治理最小 US 拆分

> 版本：v0.1  
> 日期：2026-05-06  
> 依据：[OpenMetadata_物料主数据自研服务集成设计.md](OpenMetadata_物料主数据自研服务集成设计.md) 和 [RalphLoop_物料主数据治理_PRD.md](RalphLoop_物料主数据治理_PRD.md)  
> 拆分原则：每个 US 都应独立可演示、可验收、可回滚或可补偿。优先交付端到端 tracer bullet，再逐步加厚能力。

---

## 1. Ralph Loop 拆分方法

### 1.1 拆分准则

1. **线性演练优先**：先打通“申请 -> 校验 -> 编码 -> 审批 -> Golden Record -> BTP 发布 -> OpenMetadata 同步 -> 审计”的最短路径。
2. **一条 US 只改变一个业务事实**：例如“保存草稿”和“提交申请”分开，“编码模拟”和“编码预留”分开。
3. **每条 US 都有真实验证**：至少能通过 API、页面、数据库记录、OpenMetadata 页面、BTP 回执或审计日志验证。
4. **失败路径同等重要**：BTP 发布失败、OpenMetadata 同步失败、Agent 越权、编码冲突都必须可见、可重试、可审计。
5. **上下文外置**：决策、状态、验收数据、traceId、btpTraceId、syncJobId 都落入系统或文档，不依赖口头记忆。
6. **收敛迭代**：每一轮只扩展一个闭环节点，完成后再进入下一轮。

### 1.2 US 模板

```text
作为 <角色>
我希望 <完成一个最小行为>
以便 <获得一个可验证的业务价值>

验收标准：
- Given <前置条件>
- When <用户动作或系统动作>
- Then <可观察结果>

验证方式：页面 / API / 数据库 / 审计日志 / BTP 回执 / OpenMetadata 页面
依赖：上游 US 或外部资源
```

### 1.3 Definition of Done

每条 US 完成必须满足：

- 有用户可见结果或可查询 API 结果。
- 有失败提示或异常状态。
- 有审计记录，至少包含操作者、时间、对象、动作、traceId。
- 若涉及 SAP，必须通过 BTP 或 SAP Mock/BTP Mock，不出现 SAP 直连接口。
- 若涉及 OpenMetadata，同步失败不得阻塞主流程，必须有 syncJobId 和重试入口。
- 若涉及 Agent，必须不持有 SAP 凭证，不返回 SAP 原始接口信息。

---

## 2. 交付路线总览

| Loop | 目标 | 可演示结果 | US 范围 |
|---|---|---|---|
| R0 | 骨架闭环 | 一条申请能从草稿走到审计可见的完成状态 | US-001 到 US-006 |
| R1 | 申请与模板 | 申请人能基于分类模板提交有效申请 | US-007 到 US-014 |
| R2 | 校验、重复、编码 | 提交前能校验、查重、模拟并预留编码 | US-015 到 US-024 |
| R3 | 审批与 Golden Record | 两级审批后创建可追溯黄金记录 | US-025 到 US-033 |
| R4 | BTP 受控发布 | 经 BTP 发布并记录回执，不直连 SAP | US-034 到 US-042 |
| R5 | OpenMetadata 同步 | 可见资产、术语、质量、血缘和 BTP 集成资产 | US-043 到 US-050 |
| R6 | Agent 安全能力 | Agent 可辅助但不直连 SAP | US-051 到 US-057 |
| R7 | 运营、对账、试点固化 | 形成试点可运营闭环 | US-058 到 US-064 |

---

## 3. R0 骨架闭环

### US-001 建立申请单最小状态机

**用户故事**：作为主数据管理员，我希望系统有最小申请单状态机，以便后续所有能力都能挂接到统一流程。

**验收标准**：
- Given 系统无申请单，When 创建申请单，Then 状态为 `draft`。
- Given 申请单为 `draft`，When 提交，Then 状态为 `submitted`。
- Given 申请单为 `submitted`，When 标记通过，Then 状态为 `approved`。
- Given 申请单为 `approved`，When 标记完成，Then 状态为 `completed`。

**验证方式**：API、数据库、审计日志。  
**依赖**：无。

### US-002 创建 requestId 和 traceId

**用户故事**：作为审计人员，我希望每张申请单创建时都有 requestId 和 traceId，以便后续串联全链路。

**验收标准**：
- Given 用户创建草稿，When 保存成功，Then 系统返回唯一 `requestId` 和 `traceId`。
- Given 查询申请单详情，When 输入 requestId，Then 能看到 traceId。
- Given 发生状态变更，When 查看审计日志，Then 日志包含同一个 traceId。

**验证方式**：API、审计日志。  
**依赖**：US-001。

### US-003 记录最小审计日志

**用户故事**：作为审计人员，我希望每次申请单状态变化都记录审计日志，以便追溯谁做了什么。

**验收标准**：
- Given 用户创建、提交、审批、完成申请，When 查看审计日志，Then 每个动作都有记录。
- 审计字段至少包含 actor、action、objectType、objectId、timestamp、traceId。
- 审计日志不可由普通用户修改。

**验证方式**：数据库、审计查询 API。  
**依赖**：US-002。

### US-004 建立最小发布事件表

**用户故事**：作为集成管理员，我希望审批完成后能生成发布事件，以便后续接入 BTP 发布。

**验收标准**：
- Given 申请单状态为 approved，When 触发完成动作，Then 生成 `MaterialCreated` 发布事件。
- 发布事件包含 eventId、eventType、requestId、traceId、status。
- 初始状态为 `pending`。

**验证方式**：数据库、事件查询 API。  
**依赖**：US-001、US-002。

### US-005 建立最小同步任务表

**用户故事**：作为数据治理管理员，我希望系统能记录 OpenMetadata 同步任务，以便后续异步同步可追踪。

**验收标准**：
- Given 发布事件完成，When 创建同步任务，Then 生成 syncJobId。
- 同步任务包含 requestId、targetType、status、traceId。
- 初始状态为 `pending`。

**验证方式**：数据库、同步任务查询 API。  
**依赖**：US-004。

### US-006 端到端骨架演示

**用户故事**：作为项目负责人，我希望能演示一条无真实业务规则的骨架闭环，以便确认系统路径正确。

**验收标准**：
- Given 创建一张测试申请，When 依次执行提交、审批、完成，Then 能看到申请完成、发布事件 pending、同步任务 pending。
- Given 输入 requestId，When 查看审计链路，Then 能看到创建、提交、审批、发布事件创建、同步任务创建。

**验证方式**：演示脚本、API、审计日志。  
**依赖**：US-001 到 US-005。

---

## 4. R1 申请与模板

### US-007 创建物料分类

**用户故事**：作为主数据管理员，我希望创建物料大类和小类，以便申请人按分类填写物料。

**验收标准**：
- Given 管理员创建大类“原材料”和小类“化工原料”，When 查询分类，Then 返回层级结构。
- 分类编码唯一，重复编码创建失败并提示原因。
- 分类创建动作写入审计日志。

**验证方式**：页面/API、数据库、审计日志。  
**依赖**：US-003。

### US-008 配置分类字段模板

**用户故事**：作为主数据管理员，我希望为物料小类配置字段模板，以便申请表单按分类动态展示字段。

**验收标准**：
- Given 选择“化工原料”，When 配置字段，Then 支持字段名、类型、是否必填、示例、说明。
- 字段模板保存后版本号递增。
- 模板变更写入审计日志。

**验证方式**：页面/API、数据库。  
**依赖**：US-007。

### US-009 配置标准枚举字典

**用户故事**：作为主数据管理员，我希望维护计量单位等标准枚举，以便申请字段只能选择合法值。

**验收标准**：
- Given 管理员维护计量单位 `KG`、`EA`，When 申请表加载，Then 单位字段只展示这些枚举。
- 禁用枚举值后，新申请不可选择，历史申请仍可查看原值。

**验证方式**：页面/API、数据库。  
**依赖**：US-008。

### US-010 按分类加载申请表单

**用户故事**：作为申请人，我希望选择物料分类后自动加载对应字段，以便按标准填写申请。

**验收标准**：
- Given 申请人选择“化工原料”，When 页面加载表单，Then 展示该分类模板字段。
- 必填字段有明确标识。
- 字段说明和示例可见。

**验证方式**：页面。  
**依赖**：US-008、US-009。

### US-011 保存新增物料草稿

**用户故事**：作为申请人，我希望未填完整也能保存草稿，以便分次补充资料。

**验收标准**：
- Given 必填字段未全部填写，When 点击保存草稿，Then 保存成功且状态为 `draft`。
- 草稿保存返回 requestId。
- 草稿记录已填写字段和缺失字段。

**验证方式**：页面/API、数据库。  
**依赖**：US-010。

### US-012 查看草稿详情

**用户故事**：作为申请人，我希望能打开草稿继续编辑，以便补齐字段。

**验收标准**：
- Given 已保存草稿，When 打开详情，Then 已填字段正确回显。
- Given 模板字段有说明，When 查看详情，Then 字段说明仍可见。
- 非本人或无权限用户不可编辑草稿。

**验证方式**：页面/API、权限校验。  
**依赖**：US-011。

### US-013 提交新增物料申请

**用户故事**：作为申请人，我希望字段补齐后提交申请，以便进入校验和审批流程。

**验收标准**：
- Given 必填字段已填写，When 提交申请，Then 状态从 `draft` 变为 `submitted`。
- 提交动作写入审计日志。
- 提交后申请人不可直接修改字段，只能撤回或等待退回补正。

**验证方式**：页面/API、审计日志。  
**依赖**：US-011、US-012。

### US-014 撤回已提交申请

**用户故事**：作为申请人，我希望在审批前撤回申请，以便修正明显错误。

**验收标准**：
- Given 申请处于 `submitted` 且未被审批，When 申请人撤回，Then 状态变为 `withdrawn`。
- 撤回必须填写原因。
- 撤回动作写入审计日志。

**验证方式**：页面/API、审计日志。  
**依赖**：US-013。

---

## 5. R2 校验、重复、编码

### US-015 执行必填校验

**用户故事**：作为申请人，我希望提交时系统检查必填字段，以便避免低质量申请进入审批。

**验收标准**：
- Given 必填字段缺失，When 提交，Then 提交失败并列出缺失字段。
- Given 必填字段完整，When 提交，Then 必填校验通过。
- 校验结果记录到 quality_check_result。

**验证方式**：页面/API、数据库。  
**依赖**：US-013。

### US-016 执行枚举合法性校验

**用户故事**：作为 Steward，我希望系统校验枚举值合法性，以便避免非法单位等数据进入流程。

**验收标准**：
- Given 单位不在标准字典中，When 提交，Then 返回字段级错误。
- Given 单位为启用状态枚举，When 提交，Then 校验通过。

**验证方式**：API、质量结果表。  
**依赖**：US-009、US-015。

### US-017 执行格式校验

**用户故事**：作为 Steward，我希望系统校验规格、图号等字段格式，以便减少人工退回。

**验收标准**：
- Given 字段配置了格式规则，When 值不匹配，Then 返回格式错误和示例。
- 格式规则命中结果写入质量结果表。

**验证方式**：API、质量结果表。  
**依赖**：US-008、US-015。

### US-018 查看质量校验结果

**用户故事**：作为申请人，我希望看到每条校验失败原因，以便一次性修正。

**验收标准**：
- Given 校验失败，When 查看结果，Then 每条错误包含字段、规则、当前值、修复建议。
- Given 校验通过，When 查看结果，Then 显示通过时间和质量得分。

**验证方式**：页面/API。  
**依赖**：US-015 到 US-017。

### US-019 执行精确重复预检

**用户故事**：作为 Steward，我希望提交时用图号、供应商料号、规格型号做精确查重，以便发现明显重复。

**验收标准**：
- Given 已存在相同图号记录，When 提交新申请，Then 返回高风险重复候选。
- 候选结果包含匹配字段、候选 materialCode、风险等级。

**验证方式**：API、重复预检结果表。  
**依赖**：US-013。

### US-020 执行基础模糊重复预检

**用户故事**：作为 Steward，我希望系统能发现名称和规格相似的物料，以便减少近似重复。

**验收标准**：
- Given 名称规范化后相似，When 执行预检，Then 返回相似分。
- 相似分超过阈值时标记为中风险或高风险。
- 阈值可配置。

**验证方式**：API、重复预检结果表。  
**依赖**：US-019。

### US-021 申请人说明重复候选处理意见

**用户故事**：作为申请人，我希望对重复候选填写说明，以便 Steward 判断是否继续申请。

**验收标准**：
- Given 有重复候选，When 申请人继续申请，Then 必须填写继续原因。
- 原因写入 duplicate_check_result。

**验证方式**：页面/API、数据库。  
**依赖**：US-019、US-020。

### US-022 配置基础编码规则

**用户故事**：作为主数据管理员，我希望配置物料分类对应的编码规则，以便系统自动模拟和预留编码。

**验收标准**：
- Given 管理员为“化工原料”配置编码规则，When 保存，Then 生成 ruleId 和 ruleVersion。
- 支持分类段、属性段、流水号段。
- 规则保存写入审计日志。

**验证方式**：页面/API、数据库。  
**依赖**：US-007、US-008。

### US-023 模拟物料编码

**用户故事**：作为申请人，我希望提交前看到候选编码和解释，以便理解编码来源。

**验收标准**：
- Given 申请字段满足编码规则，When 执行模拟，Then 返回候选编码。
- 返回每段编码来源、输入字段、ruleVersion。
- 模拟不占用流水号。

**验证方式**：页面/API。  
**依赖**：US-022。

### US-024 预留物料编码

**用户故事**：作为系统，我希望申请进入审批前预留编码，以便审批过程中避免并发发号冲突。

**验收标准**：
- Given 申请通过质量校验和查重处理，When 提交成功，Then 创建编码预留记录。
- 预留记录状态为 `reserved`。
- 同一分类下流水号唯一。

**验证方式**：API、数据库、并发测试。  
**依赖**：US-015 到 US-023。

---

## 6. R3 审批与 Golden Record

### US-025 创建 Steward 审批任务

**用户故事**：作为 Steward，我希望收到待审批任务，以便进行数据初审。

**验收标准**：
- Given 申请提交成功，When 查询我的任务，Then Steward 能看到待办。
- 任务包含 requestId、申请人、分类、重复风险、质量结果摘要。

**验证方式**：页面/API。  
**依赖**：US-024。

### US-026 Steward 退回补正

**用户故事**：作为 Steward，我希望能退回申请并说明原因，以便申请人补齐资料。

**验收标准**：
- Given Steward 审批任务存在，When 退回，Then 状态变为 `returned`。
- 退回意见必填。
- 编码预留保持或释放策略可配置，MVP 默认保持预留。

**验证方式**：页面/API、审计日志。  
**依赖**：US-025。

### US-027 申请人补正后重新提交

**用户故事**：作为申请人，我希望退回后能修改并重新提交，以便继续审批。

**验收标准**：
- Given 申请状态为 `returned`，When 申请人修改字段并提交，Then 状态回到 `submitted`。
- 重新提交再次执行强校验和重复预检。

**验证方式**：页面/API。  
**依赖**：US-026。

### US-028 Steward 初审通过

**用户故事**：作为 Steward，我希望确认申请通过初审，以便交给品类 Owner 审核。

**验收标准**：
- Given Steward 任务待处理，When 点击通过，Then Steward 任务完成。
- 系统创建 Owner 审批任务。
- 审批动作写入审计日志。

**验证方式**：页面/API、审计日志。  
**依赖**：US-025。

### US-029 Owner 终审通过

**用户故事**：作为品类 Owner，我希望终审通过申请，以便系统生成 Golden Record。

**验收标准**：
- Given Owner 任务待处理，When 点击通过，Then 申请状态变为 `approved`。
- 终审意见可选，驳回意见必填。
- 动作写入审计日志。

**验证方式**：页面/API、审计日志。  
**依赖**：US-028。

### US-030 Owner 驳回申请并释放编码

**用户故事**：作为品类 Owner，我希望驳回不合理申请，以便释放预留编码并结束流程。

**验收标准**：
- Given Owner 驳回，When 保存意见，Then 申请状态变为 `rejected`。
- 预留编码状态变为 `released`。
- 驳回原因和释放记录可追溯。

**验证方式**：页面/API、数据库。  
**依赖**：US-029。

### US-031 确认预留编码

**用户故事**：作为系统，我希望终审通过后确认预留编码，以便形成正式物料编码。

**验收标准**：
- Given 申请终审通过且编码为 reserved，When 确认编码，Then 编码状态变为 `confirmed`。
- 编码历史记录 ruleVersion、输入属性、确认时间。

**验证方式**：API、数据库。  
**依赖**：US-029。

### US-032 创建 Golden Record V1

**用户故事**：作为主数据管理员，我希望审批通过后系统创建 Golden Record，以便形成物料治理事实。

**验收标准**：
- Given 编码确认成功，When 创建 Golden Record，Then 生成 materialCode 和版本 1。
- 关键字段来自申请单当前值。
- 状态为 `effective_pending_publish`。

**验证方式**：API、数据库。  
**依赖**：US-031。

### US-033 记录字段级来源

**用户故事**：作为审计人员，我希望 Golden Record 每个关键字段都有来源说明，以便后续追溯。

**验收标准**：
- Given Golden Record 创建，When 查询字段来源，Then materialName、category、baseUom、specification 至少有来源记录。
- 来源记录包含 requestId、sourceType、confidence、approvedAt。

**验证方式**：API、数据库。  
**依赖**：US-032。

---

## 7. R4 BTP 受控发布

### US-034 生成 MaterialCreated Outbox 事件

**用户故事**：作为集成管理员，我希望 Golden Record 创建后生成 Outbox 事件，以便可靠发布。

**验收标准**：
- Given Golden Record V1 创建成功，When 事务提交，Then 生成 `MaterialCreated` 事件。
- 事件包含 requestId、materialCode、version、traceId。
- 事件状态为 `pending`。

**验证方式**：数据库、事件查询 API。  
**依赖**：US-032。

### US-035 配置 BTP 发布目标

**用户故事**：作为 SAP 集成管理员，我希望配置 BTP 发布目标，以便发布服务只调用受控 API。

**验收标准**：
- Given 管理员配置 BTP API Proxy 地址和 Scope，When 保存，Then 目标状态为 enabled。
- 配置不保存明文密钥。
- 配置变更写入审计日志。

**验证方式**：配置页面/API、审计日志。  
**依赖**：US-003。

### US-036 调用 BTP 发布 API

**用户故事**：作为系统，我希望发布事件由发布服务调用 BTP API，以便遵守 SAP 不直连约束。

**验收标准**：
- Given 有 pending 发布事件，When 发布任务执行，Then 调用 BTP API Proxy。
- 发布记录不包含 SAP 后端直连接口地址。
- 发布请求包含 traceId 和 requestId。

**验证方式**：发布日志、配置检查、接口 Mock。  
**依赖**：US-034、US-035。

### US-037 记录 BTP 发布成功回执

**用户故事**：作为主数据管理员，我希望看到 BTP 发布成功和 SAP 物料号，以便确认物料已下发。

**验收标准**：
- Given BTP 返回成功，When 发布任务完成，Then 记录 btpTraceId、sapMaterialNo、publishedAt。
- Golden Record 状态变为 `effective_published`。
- material_cross_reference 写入企业编码和 SAP 物料号映射。

**验证方式**：页面/API、数据库。  
**依赖**：US-036。

### US-038 记录 BTP 发布失败

**用户故事**：作为 SAP 集成管理员，我希望发布失败时看到错误摘要，以便修复集成问题。

**验收标准**：
- Given BTP 返回失败，When 发布任务结束，Then 发布状态为 `failed`。
- 记录 btpTraceId、errorCode、errorMessage、retryable。
- Golden Record 状态变为 `publish_failed`。

**验证方式**：页面/API、数据库。  
**依赖**：US-036。

### US-039 重试 BTP 发布失败任务

**用户故事**：作为 SAP 集成管理员，我希望能重试可恢复的发布失败任务，以便完成下发。

**验收标准**：
- Given 发布任务为 failed 且 retryable，When 点击重试，Then 生成新 attempt。
- 每次 attempt 保留原错误和新 btpTraceId。
- 重试成功后状态变为 published。

**验证方式**：页面/API、发布记录。  
**依赖**：US-038。

### US-040 查看发布任务详情

**用户故事**：作为主数据管理员，我希望按 requestId 查看发布详情，以便处理异常。

**验收标准**：
- Given 输入 requestId，When 查看发布任务，Then 展示事件状态、attempt、btpTraceId、SAP 回执。
- 普通申请人只能看摘要，集成管理员可看错误详情。

**验证方式**：页面/API、权限校验。  
**依赖**：US-037、US-038。

### US-041 审计 BTP 发布动作

**用户故事**：作为审计人员，我希望 BTP 发布动作进入审计链，以便证明没有直连 SAP。

**验收标准**：
- Given 发布服务调用 BTP，When 查看审计链，Then 出现 `btp_publish_requested` 动作。
- Given BTP 返回，When 查看审计链，Then 出现 `btp_publish_completed` 或 `btp_publish_failed`。
- 审计记录包含 btpTraceId。

**验证方式**：审计查询 API。  
**依赖**：US-036 到 US-038。

### US-042 校验无 SAP 直连配置

**用户故事**：作为合规人员，我希望系统能检查是否存在 SAP 直连配置，以便满足政策约束。

**验收标准**：
- Given 查看集成配置，When 执行合规检查，Then 不存在 SAP 后端直连地址。
- 若发现疑似直连配置，Then 标记为 high risk 并禁止启用。

**验证方式**：配置扫描、审计报告。  
**依赖**：US-035。

---

## 8. R5 OpenMetadata 同步

### US-043 同步 Golden Record 表资产

**用户故事**：作为数据治理管理员，我希望 Golden Record 表同步到 OpenMetadata，以便在数据目录中可见。

**验收标准**：
- Given Golden Record 表存在，When 执行同步，Then OpenMetadata 中出现表资产。
- 表资产包含字段、Owner、Domain。
- 同步任务记录 syncJobId。

**验证方式**：OpenMetadata 页面/API、同步任务表。  
**依赖**：US-032。

### US-044 同步物料术语

**用户故事**：作为数据治理管理员，我希望物料分类和字段术语同步到 OpenMetadata，以便统一口径。

**验收标准**：
- Given 分类模板存在，When 同步术语，Then OpenMetadata Glossary 中出现物料、物料分类、计量单位。
- 术语与 Golden Record 字段可关联。

**验证方式**：OpenMetadata 页面/API。  
**依赖**：US-007、US-008。

### US-045 同步质量结果

**用户故事**：作为数据治理管理员，我希望质量校验结果同步到 OpenMetadata，以便形成质量看板。

**验收标准**：
- Given 申请有质量校验结果，When 同步，Then OpenMetadata 可见质量规则或质量结果资产。
- 同步内容不包含每条敏感物料明细，只包含规则结果和摘要。

**验证方式**：OpenMetadata 页面/API。  
**依赖**：US-018。

### US-046 同步 BTP API 资产

**用户故事**：作为数据治理管理员，我希望 BTP API Product/API Proxy 同步到 OpenMetadata，以便治理 SAP 受控访问入口。

**验收标准**：
- Given BTP 发布目标已配置，When 同步 BTP API 资产，Then OpenMetadata 中出现 API 资产摘要。
- 资产记录 Owner、Scope、版本、用途。
- 不同步 SAP 技术凭证。

**验证方式**：OpenMetadata 页面/API、配置检查。  
**依赖**：US-035。

### US-047 同步发布血缘摘要

**用户故事**：作为数据治理管理员，我希望看到 Golden Record 经 BTP 发布到 SAP 的血缘摘要，以便理解数据流向。

**验收标准**：
- Given 发布成功，When 同步血缘，Then OpenMetadata 展示 Golden Record -> BTP API -> SAP 目标的血缘摘要。
- 血缘来源为 BTP 回执和发布事件，不直连 SAP。

**验证方式**：OpenMetadata 血缘页面。  
**依赖**：US-037、US-046。

### US-048 OpenMetadata 同步失败可见

**用户故事**：作为数据治理管理员，我希望同步失败时可见错误，以便修复同步问题。

**验收标准**：
- Given OpenMetadata API 返回失败，When 同步任务结束，Then 状态为 `failed`。
- 记录 errorCode、errorMessage、retryable。
- 不影响 Golden Record 和 BTP 发布状态。

**验证方式**：同步任务页面/API、状态检查。  
**依赖**：US-043。

### US-049 重试 OpenMetadata 同步

**用户故事**：作为数据治理管理员，我希望能重试失败同步任务，以便最终一致。

**验收标准**：
- Given 同步任务 failed 且 retryable，When 点击重试，Then 生成新 attempt。
- 重试成功后状态为 `succeeded`。
- 原错误记录保留。

**验证方式**：同步任务页面/API。  
**依赖**：US-048。

### US-050 按 requestId 查看治理可见性

**用户故事**：作为项目负责人，我希望按 requestId 查看 OpenMetadata 同步结果，以便验收治理闭环。

**验收标准**：
- Given 输入 requestId，When 查看治理可见性，Then 展示资产、术语、质量、血缘、BTP API 同步状态。
- 每个同步项展示 syncJobId 和状态。

**验证方式**：页面/API、OpenMetadata 页面跳转。  
**依赖**：US-043 到 US-049。

---

## 9. R6 Agent 安全能力

### US-051 创建 Agent 工具白名单

**用户故事**：作为安全管理员，我希望配置 Agent 可调用工具白名单，以便禁止 Agent 任意调用接口。

**验收标准**：
- Given 管理员启用 `search-similar` 和 `draft-request`，When Agent 调用未授权工具，Then 请求被拒绝。
- 白名单变更写入审计日志。

**验证方式**：API、审计日志。  
**依赖**：US-003。

### US-052 Agent 查询相似物料

**用户故事**：作为申请人，我希望 Agent 帮我查询相似物料，以便避免重复创建。

**验收标准**：
- Given 用户输入物料描述，When Agent 调用相似物料工具，Then 返回候选列表和相似原因。
- 返回结果受当前用户权限裁剪。
- 不返回 SAP 原始接口、SAP 后端表名或技术凭证。

**验证方式**：Agent 工具 API、审计日志。  
**依赖**：US-019、US-020、US-051。

### US-053 Agent 推荐物料分类

**用户故事**：作为申请人，我希望 Agent 推荐物料分类，以便减少填单错误。

**验收标准**：
- Given 用户输入物料描述，When Agent 推荐分类，Then 返回推荐分类、置信度、理由。
- 用户必须手动确认推荐结果。
- 推荐结果标记为“建议”。

**验证方式**：Agent 工具 API、页面。  
**依赖**：US-007、US-051。

### US-054 Agent 推荐字段值

**用户故事**：作为申请人，我希望 Agent 根据描述推荐字段值，以便快速生成草稿。

**验收标准**：
- Given 用户确认分类，When Agent 推荐字段，Then 返回字段建议和理由。
- 必填字段未能推断时标记为需人工填写。

**验证方式**：Agent 工具 API、页面。  
**依赖**：US-008、US-053。

### US-055 Agent 生成草稿

**用户故事**：作为申请人，我希望 Agent 根据确认后的建议生成草稿，以便继续人工提交。

**验收标准**：
- Given 用户确认建议，When Agent 创建草稿，Then 系统生成 `draft` 状态申请。
- Agent 不自动提交申请。
- 草稿记录 createdByAgent、confirmedByUser。

**验证方式**：页面/API、审计日志。  
**依赖**：US-011、US-054。

### US-056 记录 Agent 工具调用审计

**用户故事**：作为审计人员，我希望所有 Agent 工具调用都可追溯，以便证明 Agent 没有直连 SAP。

**验收标准**：
- Given Agent 调用任一工具，When 查看审计，Then 有 userId、toolName、inputSummary、outputSummary、traceId。
- 审计记录标明是否访问 BTP 业务语义 API。
- 审计记录不保存敏感明文凭证。

**验证方式**：审计查询 API。  
**依赖**：US-051 到 US-055。

### US-057 拦截 Agent 越权请求

**用户故事**：作为安全管理员，我希望 Agent 越权访问被拦截，以便满足 SAP 不直连政策和最小权限原则。

**验收标准**：
- Given Agent 请求 SAP 原始接口信息，When 调用工具，Then 系统拒绝并返回安全错误。
- Given Agent 请求批量导出超权限字段，When 调用工具，Then 系统拒绝并写审计。

**验证方式**：安全测试、审计日志。  
**依赖**：US-051、US-056。

---

## 10. R7 运营、对账、试点固化

### US-058 按 requestId 查看全链路追踪

**用户故事**：作为审计人员，我希望按 requestId 查看申请、审批、编码、发布、同步和 Agent 调用，以便完成闭环审计。

**验收标准**：
- Given 输入 requestId，When 查看追踪，Then 展示状态时间线。
- 时间线包含 traceId、materialCode、btpTraceId、syncJobId。

**验证方式**：审计页面/API。  
**依赖**：US-041、US-050、US-056。

### US-059 建立治理运营看板

**用户故事**：作为数据治理负责人，我希望看到申请、退回、重复、发布和同步指标，以便持续运营。

**验收标准**：
- 看板展示申请数量、退回率、重复候选数、BTP 发布成功率、OpenMetadata 同步成功率。
- 指标可按时间和物料分类筛选。

**验证方式**：看板页面、统计 API。  
**依赖**：US-050、US-058。

### US-060 对账 BTP 与自研发布记录

**用户故事**：作为 SAP 集成管理员，我希望对账 BTP 回执和自研发布记录，以便发现状态不一致。

**验收标准**：
- Given 发布任务存在，When 执行对账，Then 比较 publish_event、btp_publish_trace 状态。
- 不一致时生成对账异常。

**验证方式**：对账任务、异常列表。  
**依赖**：US-037、US-038。

### US-061 对账 OpenMetadata 与自研资产记录

**用户故事**：作为数据治理管理员，我希望对账 OpenMetadata 同步结果和自研资产记录，以便保证治理目录可信。

**验收标准**：
- Given 同步任务成功，When 执行对账，Then 检查资产、术语、血缘是否存在。
- 缺失时生成同步补偿任务。

**验证方式**：对账任务、OpenMetadata API。  
**依赖**：US-043 到 US-050。

### US-062 生成试点验收报告

**用户故事**：作为项目负责人，我希望生成试点验收报告，以便管理层确认是否推广。

**验收标准**：
- 报告包含闭环成功案例、失败案例、指标、风险、待决事项。
- 报告列出是否存在第三方直连 SAP 路径，目标为 0。

**验证方式**：报告导出、人工评审。  
**依赖**：US-059 到 US-061。

### US-063 建立试点数据集

**用户故事**：作为测试负责人，我希望维护一组试点物料数据，以便每轮发布可重复验证。

**验收标准**：
- 数据集包含正常新增、字段缺失、重复物料、BTP 失败、OpenMetadata 失败、Agent 越权案例。
- 每个案例有预期结果。

**验证方式**：测试数据文件、演示脚本。  
**依赖**：R0 到 R6 全部闭环。

### US-064 建立发布前检查清单

**用户故事**：作为发布负责人，我希望有发布前检查清单，以便每次上线前确认风险可控。

**验收标准**：
- 清单包含数据库迁移、BTP 配置、OpenMetadata Token、Agent 白名单、审计开关、回滚方案。
- 每项有负责人和完成状态。

**验证方式**：发布清单、人工评审。  
**依赖**：US-062、US-063。

---

## 11. 优先级建议

### 11.1 必须先做

| 优先级 | US |
|---|---|
| P0 | US-001 到 US-006，建立骨架闭环 |
| P0 | US-007 到 US-014，建立申请入口 |
| P0 | US-015、US-018、US-022、US-023、US-024，建立可提交、可编码的最小路径 |
| P0 | US-025、US-028、US-029、US-031、US-032、US-034、US-036、US-037，建立审批到 BTP 发布闭环 |
| P0 | US-043、US-047、US-050，建立 OpenMetadata 最小可见性 |

### 11.2 可后置但不应删除

| 优先级 | US |
|---|---|
| P1 | US-019 到 US-021，重复预检体验增强 |
| P1 | US-038 到 US-041，发布失败和审计增强 |
| P1 | US-048、US-049，同步失败恢复 |
| P1 | US-051 到 US-057，Agent 安全能力 |
| P2 | US-058 到 US-064，运营、对账、试点固化 |

---

## 12. 每轮演示脚本

| Loop | 演示脚本 |
|---|---|
| R0 | 创建测试申请，手动推进状态，看到发布事件、同步任务和审计链 |
| R1 | 配置分类模板，申请人保存草稿、回显草稿、提交申请 |
| R2 | 提交时触发校验、重复预检、编码模拟和编码预留 |
| R3 | Steward 初审、Owner 终审，生成 Golden Record V1 和字段来源 |
| R4 | Golden Record 生成 MaterialCreated 事件，经 BTP Mock 发布，记录 btpTraceId |
| R5 | 同步到 OpenMetadata，看到表资产、术语、质量、血缘和 BTP API 资产 |
| R6 | Agent 查询相似物料并生成草稿，越权请求被拦截并审计 |
| R7 | 按 requestId 查看全链路，生成试点报告和发布前检查清单 |

---

## 13. 开发拆分建议

### 13.1 小提交原则

- 每个 US 至少一个独立提交，复杂 US 可拆成 API、页面、测试三个提交。
- 不把“数据库结构、业务逻辑、页面、集成、同步”全塞进一个提交。
- 每个提交必须能跑通已有测试或至少不破坏前一轮演示脚本。

### 13.2 测试建议

| 测试类型 | 覆盖内容 |
|---|---|
| 单元测试 | 编码规则、状态机、质量规则、权限判断 |
| 集成测试 | 申请提交、审批、Golden Record 创建、Outbox 事件 |
| 契约测试 | BTP API Mock、OpenMetadata API Mock、Agent 工具 API |
| 端到端测试 | MVP 演示脚本 |
| 安全测试 | Agent 越权、SAP 直连配置扫描、Token 权限 |

### 13.3 上线门禁

R4 之后每次上线必须检查：

- 是否存在绕过 BTP 的 SAP 直连配置。
- BTP API Scope 是否最小化。
- OpenMetadata Bot Token 是否只具备所需权限。
- Agent 工具白名单是否符合当前发布范围。
- 发布失败和同步失败是否可重试。
- 全链路审计是否能按 requestId 串联。